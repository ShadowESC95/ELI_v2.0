# Windows scripts stored byte-for-byte (CRLF) — cmd.exe goto-label lookup fails on LF-only
# .bat. The Windows build also force-converts, so CRLF is guaranteed either way.
*.bat -text
*.cmd -text
*.ps1 -text
*.sh text eol=lf
