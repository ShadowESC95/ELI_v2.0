# ELI MKXI Android / Termux profile.
# Android cannot provide full desktop parity for PySide6 GUI, global desktop
# control, PyAudio, CUDA, AppImage, Windows COM audio, or macOS automation.
# This profile is for the portable/headless runtime and Termux integrations.

platformdirs>=4.9.6
pydantic>=2.13
pydantic-settings>=2.14
requests>=2.33
Pillow>=12.0
python-dotenv>=1.0
PyYAML>=6.0
tqdm>=4.67
psutil>=7.0
numpy>=2.0
packaging>=26
typing_extensions>=4.15

# Text, documents, and HTTP tools
markdown>=3.5
pypdf>=4.0
python-docx>=1.1
python-dateutil>=2.9
six>=1.17
filelock>=3.29
fsspec>=2026
regex>=2026
rich>=15.0
click>=8.3
arrow>=1.4
httpx>=0.28
certifi>=2026
charset-normalizer>=3.4
idna>=3.13
urllib3>=2.6

# Optional Android notes:
# - Use Termux packages for OS integration:
#   pkg install termux-api
# - Install native/large AI packages manually only if your device supports them.
# - llama-cpp-python, torch, faiss-cpu, PySide6, PyAudio, sounddevice, and
#   faster-whisper may need custom Android builds and are intentionally excluded.
