# Offline-first wheels bundled in linux/windows portable packages.
# Ensures the graphical installer and GUI launch without a network round-trip
# for Qt bindings. Full runtime deps still come from install.sh (wheelhouse +
# PyPI fallback). Keep this list small — release assets have a size budget.
PySide6>=6.6.0
shiboken6>=6.6.0
