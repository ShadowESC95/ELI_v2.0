diff --git a/.capability_snapshot.json b/.capability_snapshot.json
index 37a5578..525b164 100644
--- a/.capability_snapshot.json
+++ b/.capability_snapshot.json
@@ -189,6 +189,13 @@
       "routable": true,
       "source": "executor+router"
     },
+    "FOCUS_APP": {
+      "in_dispatch": true,
+      "in_supported_list": true,
+      "plugin": null,
+      "routable": true,
+      "source": "executor+router"
+    },
     "GENERATE_DOCUMENT": {
       "in_dispatch": true,
       "in_supported_list": true,
@@ -322,6 +329,13 @@
       "routable": true,
       "source": "executor+router+plugin:notes"
     },
+    "MAXIMISE_WINDOW": {
+      "in_dispatch": true,
+      "in_supported_list": true,
+      "plugin": null,
+      "routable": false,
+      "source": "executor"
+    },
     "MEDIA_CONTROL": {
       "in_dispatch": true,
       "in_supported_list": true,
@@ -357,6 +371,13 @@
       "routable": true,
       "source": "executor+router"
     },
+    "MINIMISE_ALL": {
+      "in_dispatch": true,
+      "in_supported_list": true,
+      "plugin": null,
+      "routable": false,
+      "source": "executor"
+    },
     "MORNING_REPORT": {
       "in_dispatch": true,
       "in_supported_list": true,
@@ -392,6 +413,13 @@
       "routable": true,
       "source": "executor+router"
     },
+    "NEXT_WINDOW": {
+      "in_dispatch": true,
+      "in_supported_list": true,
+      "plugin": null,
+      "routable": false,
+      "source": "executor"
+    },
     "NOOP": {
       "in_dispatch": false,
       "in_supported_list": false,
@@ -609,6 +637,13 @@
       "routable": true,
       "source": "executor+router"
     },
+    "PREVIOUS_WINDOW": {
+      "in_dispatch": true,
+      "in_supported_list": true,
+      "plugin": null,
+      "routable": false,
+      "source": "executor"
+    },
     "PROACTIVE_START": {
       "in_dispatch": true,
       "in_supported_list": true,
@@ -665,6 +700,13 @@
       "routable": true,
       "source": "executor+router"
     },
+    "RESTORE_WINDOWS": {
+      "in_dispatch": true,
+      "in_supported_list": true,
+      "plugin": null,
+      "routable": false,
+      "source": "executor"
+    },
     "RUNTIME_AUDIT": {
       "in_dispatch": true,
       "in_supported_list": true,
@@ -693,6 +735,13 @@
       "routable": true,
       "source": "executor+router"
     },
+    "SCREEN_LOCATE": {
+      "in_dispatch": true,
+      "in_supported_list": true,
+      "plugin": null,
+      "routable": true,
+      "source": "executor+router"
+    },
     "SCREEN_READ_ANALYZE": {
       "in_dispatch": true,
       "in_supported_list": true,
@@ -854,6 +903,13 @@
       "routable": true,
       "source": "executor+router"
     },
+    "SWITCH_WORKSPACE": {
+      "in_dispatch": true,
+      "in_supported_list": true,
+      "plugin": null,
+      "routable": false,
+      "source": "executor"
+    },
     "SYSTEM_STATS": {
       "in_dispatch": true,
       "in_supported_list": true,
@@ -861,6 +917,13 @@
       "routable": true,
       "source": "executor+router+plugin:system_stats"
     },
+    "TILE_WINDOWS": {
+      "in_dispatch": true,
+      "in_supported_list": true,
+      "plugin": null,
+      "routable": false,
+      "source": "executor"
+    },
     "TIME": {
       "in_dispatch": true,
       "in_supported_list": true,
@@ -918,6 +981,6 @@
       "source": "executor+router"
     }
   },
-  "count": 131,
-  "generated_at": 1777652056.1097605
+  "count": 140,
+  "generated_at": 1777845991.2896442
 }
diff --git a/capability_manifest.json b/capability_manifest.json
index c050482..9281810 100644
--- a/capability_manifest.json
+++ b/capability_manifest.json
@@ -1,6 +1,6 @@
 {
-  "generated_at": "2026-05-01T19:30:11",
-  "total": 131,
+  "generated_at": "2026-05-04T20:09:40",
+  "total": 140,
   "capabilities": [
     {
       "action": "ADD_EVENT",
@@ -245,6 +245,15 @@
       "in_dispatch": true,
       "in_supported_list": true
     },
+    {
+      "action": "FOCUS_APP",
+      "source": "executor+router",
+      "active": true,
+      "plugin": null,
+      "routable": true,
+      "in_dispatch": true,
+      "in_supported_list": true
+    },
     {
       "action": "GENERATE_DOCUMENT",
       "source": "executor+router",
@@ -416,6 +425,15 @@
       "in_dispatch": true,
       "in_supported_list": true
     },
+    {
+      "action": "MAXIMISE_WINDOW",
+      "source": "executor",
+      "active": true,
+      "plugin": null,
+      "routable": false,
+      "in_dispatch": true,
+      "in_supported_list": true
+    },
     {
       "action": "MEDIA_CONTROL",
       "source": "executor+router",
@@ -461,6 +479,15 @@
       "in_dispatch": true,
       "in_supported_list": true
     },
+    {
+      "action": "MINIMISE_ALL",
+      "source": "executor",
+      "active": true,
+      "plugin": null,
+      "routable": false,
+      "in_dispatch": true,
+      "in_supported_list": true
+    },
     {
       "action": "MORNING_REPORT",
       "source": "executor+router",
@@ -506,6 +533,15 @@
       "in_dispatch": true,
       "in_supported_list": true
     },
+    {
+      "action": "NEXT_WINDOW",
+      "source": "executor",
+      "active": true,
+      "plugin": null,
+      "routable": false,
+      "in_dispatch": true,
+      "in_supported_list": true
+    },
     {
       "action": "NOOP",
       "source": "router_only",
@@ -785,6 +821,15 @@
       "in_dispatch": true,
       "in_supported_list": true
     },
+    {
+      "action": "PREVIOUS_WINDOW",
+      "source": "executor",
+      "active": true,
+      "plugin": null,
+      "routable": false,
+      "in_dispatch": true,
+      "in_supported_list": true
+    },
     {
       "action": "PROACTIVE_START",
       "source": "executor+router",
@@ -857,6 +902,15 @@
       "in_dispatch": true,
       "in_supported_list": true
     },
+    {
+      "action": "RESTORE_WINDOWS",
+      "source": "executor",
+      "active": true,
+      "plugin": null,
+      "routable": false,
+      "in_dispatch": true,
+      "in_supported_list": true
+    },
     {
       "action": "RUNTIME_AUDIT",
       "source": "executor+router",
@@ -893,6 +947,15 @@
       "in_dispatch": true,
       "in_supported_list": true
     },
+    {
+      "action": "SCREEN_LOCATE",
+      "source": "executor+router",
+      "active": true,
+      "plugin": null,
+      "routable": true,
+      "in_dispatch": true,
+      "in_supported_list": true
+    },
     {
       "action": "SCREEN_READ_ANALYZE",
       "source": "executor+router",
@@ -1100,6 +1163,15 @@
       "in_dispatch": true,
       "in_supported_list": true
     },
+    {
+      "action": "SWITCH_WORKSPACE",
+      "source": "executor",
+      "active": true,
+      "plugin": null,
+      "routable": false,
+      "in_dispatch": true,
+      "in_supported_list": true
+    },
     {
       "action": "SYSTEM_STATS",
       "source": "executor+router+plugin:system_stats",
@@ -1109,6 +1181,15 @@
       "in_dispatch": true,
       "in_supported_list": true
     },
+    {
+      "action": "TILE_WINDOWS",
+      "source": "executor",
+      "active": true,
+      "plugin": null,
+      "routable": false,
+      "in_dispatch": true,
+      "in_supported_list": true
+    },
     {
       "action": "TIME",
       "source": "executor+router",
diff --git a/config/quick_actions.json b/config/quick_actions.json
index ff14d5f..3cb4719 100644
--- a/config/quick_actions.json
+++ b/config/quick_actions.json
@@ -40,4 +40,4 @@
   "PERSONA_LOCK_STATUS",
   "LIST_CAPABILITIES",
   "SELF_REPORT"
-]
+]
\ No newline at end of file
diff --git a/config/settings.json b/config/settings.json
index a104fea..24349c1 100644
--- a/config/settings.json
+++ b/config/settings.json
@@ -1,38 +1,50 @@
 {
-  "n_ctx": 16384,
-  "n_gpu_layers": 11,
-  "n_threads": 10,
-  "batch_size": 512,
-  "max_tokens": -1,
-  "temperature": 0.7,
-  "model_path": "models/gguf/base/openhermes-2.5-mistral-7b.Q3_K_M.gguf",
-  "bundled_model_path": "models/gguf/base/openhermes-2.5-mistral-7b.Q3_K_M.gguf",
-  "first_run_complete": true,
-  "custom_model_path": "models/gguf/base/openhermes-2.5-mistral-7b.Q3_K_M.gguf",
-  "gguf_model_path": "models/gguf/base/openhermes-2.5-mistral-7b.Q3_K_M.gguf",
   "provider": "custom_gguf",
+  "model_path": "models/mistral-7b-instruct-v0.2.Q3_K_M.gguf",
+  "bundled_model_path": "models/mistral-7b-instruct-v0.2.Q3_K_M.gguf",
+  "custom_model_path": "models/mistral-7b-instruct-v0.2.Q3_K_M.gguf",
   "ollama_host": "http://localhost:11434",
   "ollama_model": "",
   "persona_file": "",
   "user_name": "",
   "user_text_color": "#4DA3FF",
+  "image_style_profile": "auto",
+  "image_palette_profile": "auto",
+  "image_profile_notes": "",
+  "image_backend": "auto",
+  "image_model_path": "",
+  "image_device": "auto",
+  "image_quality_preset": "ultra",
+  "image_steps": 36,
+  "image_guidance": 7.2,
+  "image_negative_prompt": "",
+  "image_default_project_path": "",
+  "image_default_count": 1,
+  "image_default_width": 1400,
+  "image_default_height": 900,
+  "image_auto_personalize": true,
+  "image_auto_open": true,
+  "image_use_chat_context": true,
+  "image_use_proactive_context": true,
+  "n_ctx": 8192,
+  "max_tokens": 512,
+  "temperature": 0.7,
   "top_p": 0.95,
   "top_k": 40,
   "repeat_penalty": 1.1,
+  "n_gpu_layers": 13,
+  "n_threads": 4,
+  "batch_size": 128,
   "use_mmap": true,
   "use_mlock": true,
   "auto_speak": false,
+  "tts_voice": "en_US-amy-medium",
   "mic_enabled": false,
   "auto_save": true,
   "log_to_file": false,
   "auto_load": true,
+  "first_run_complete": true,
   "theme": "dark",
-  "image_backend": "diffusion",
-  "image_model_path": "models/image/ssd-1b",
-  "image_device": "cuda",
-  "image_quality_preset": "ultra",
-  "image_steps": 36,
-  "image_guidance": 7.2,
-  "image_default_count": 1,
-  "image_auto_open": true
-}
+  "gguf_model_path": "models/mistral-7b-instruct-v0.2.Q3_K_M.gguf",
+  "hardware_fingerprint": "83e0735ae1fa3ada"
+}
\ No newline at end of file
diff --git a/eli/capability_inventory.generated.json b/eli/capability_inventory.generated.json
index e0ea647..07a39a8 100644
--- a/eli/capability_inventory.generated.json
+++ b/eli/capability_inventory.generated.json
@@ -216,6 +216,14 @@
       "routable": true,
       "source": "executor+router"
     },
+    {
+      "action": "FOCUS_APP",
+      "in_dispatch": true,
+      "in_supported_list": true,
+      "plugin": null,
+      "routable": true,
+      "source": "executor+router"
+    },
     {
       "action": "GENERATE_DOCUMENT",
       "in_dispatch": true,
@@ -368,6 +376,14 @@
       "routable": true,
       "source": "executor+router+plugin:notes"
     },
+    {
+      "action": "MAXIMISE_WINDOW",
+      "in_dispatch": true,
+      "in_supported_list": true,
+      "plugin": null,
+      "routable": false,
+      "source": "executor"
+    },
     {
       "action": "MEDIA_CONTROL",
       "in_dispatch": true,
@@ -408,6 +424,14 @@
       "routable": true,
       "source": "executor+router"
     },
+    {
+      "action": "MINIMISE_ALL",
+      "in_dispatch": true,
+      "in_supported_list": true,
+      "plugin": null,
+      "routable": false,
+      "source": "executor"
+    },
     {
       "action": "MORNING_REPORT",
       "in_dispatch": true,
@@ -448,6 +472,14 @@
       "routable": true,
       "source": "executor+router"
     },
+    {
+      "action": "NEXT_WINDOW",
+      "in_dispatch": true,
+      "in_supported_list": true,
+      "plugin": null,
+      "routable": false,
+      "source": "executor"
+    },
     {
       "action": "NOOP",
       "in_dispatch": false,
@@ -696,6 +728,14 @@
       "routable": true,
       "source": "executor+router"
     },
+    {
+      "action": "PREVIOUS_WINDOW",
+      "in_dispatch": true,
+      "in_supported_list": true,
+      "plugin": null,
+      "routable": false,
+      "source": "executor"
+    },
     {
       "action": "PROACTIVE_START",
       "in_dispatch": true,
@@ -760,6 +800,14 @@
       "routable": true,
       "source": "executor+router"
     },
+    {
+      "action": "RESTORE_WINDOWS",
+      "in_dispatch": true,
+      "in_supported_list": true,
+      "plugin": null,
+      "routable": false,
+      "source": "executor"
+    },
     {
       "action": "RUNTIME_AUDIT",
       "in_dispatch": true,
@@ -792,6 +840,14 @@
       "routable": true,
       "source": "executor+router"
     },
+    {
+      "action": "SCREEN_LOCATE",
+      "in_dispatch": true,
+      "in_supported_list": true,
+      "plugin": null,
+      "routable": true,
+      "source": "executor+router"
+    },
     {
       "action": "SCREEN_READ_ANALYZE",
       "in_dispatch": true,
@@ -976,6 +1032,14 @@
       "routable": true,
       "source": "executor+router"
     },
+    {
+      "action": "SWITCH_WORKSPACE",
+      "in_dispatch": true,
+      "in_supported_list": true,
+      "plugin": null,
+      "routable": false,
+      "source": "executor"
+    },
     {
       "action": "SYSTEM_STATS",
       "in_dispatch": true,
@@ -984,6 +1048,14 @@
       "routable": true,
       "source": "executor+router+plugin:system_stats"
     },
+    {
+      "action": "TILE_WINDOWS",
+      "in_dispatch": true,
+      "in_supported_list": true,
+      "plugin": null,
+      "routable": false,
+      "source": "executor"
+    },
     {
       "action": "TIME",
       "in_dispatch": true,
@@ -1049,7 +1121,7 @@
       "source": "executor+router"
     }
   ],
-  "count": 131,
-  "generated_at": 1777660211.9101214,
+  "count": 140,
+  "generated_at": 1777921780.3631713,
   "generator": "brain.awareness.capability_sync"
 }
diff --git a/eli/cognition/context_synthesiser.py b/eli/cognition/context_synthesiser.py
index 2e2c909..2979263 100644
--- a/eli/cognition/context_synthesiser.py
+++ b/eli/cognition/context_synthesiser.py
@@ -37,6 +37,33 @@ MIN_VECTOR_SCORE   = 0.35
 MAX_MEMORY_ITEMS   = 6
 
 
+
+# ELI_CONTEXT_VECTOR_GUARD_20260502
+def _eli_skip_vector_recall_for_query(query: str) -> bool:
+    low = re.sub(r"\s+", " ", str(query or "").lower()).strip(" .,!?:;")
+    if not low:
+        return True
+
+    if re.search(
+        r"\b("
+        r"who'?s talking|who is talking|who'?s speaking|who is speaking|"
+        r"what am i hearing|what are you hearing|are you hearing me|"
+        r"what did you hear|is spotify talking|is youtube talking"
+        r")\b",
+        low,
+    ):
+        return True
+
+    memory_markers = (
+        "memory", "remember", "recall", "previous", "earlier", "last time",
+        "what do you know about me", "who am i", "my name", "profile",
+    )
+    if any(m in low for m in memory_markers):
+        return False
+
+    return len(re.findall(r"[a-z0-9']+", low)) < 8
+
+
 class ContextSynthesiser:
     def synthesise(self, user_input: str = "", memory_context: str = "",
                    bus_result: Optional[Any] = None,
@@ -113,6 +140,8 @@ class ContextSynthesiser:
 
     @staticmethod
     def _build_vector_block(query: str) -> str:
+        if _eli_skip_vector_recall_for_query(query):
+            return ""
         if not query.strip():
             return ""
         try:
diff --git a/eli/cognition/persona.txt b/eli/cognition/persona.txt
index 1c14460..9fb28ed 100644
--- a/eli/cognition/persona.txt
+++ b/eli/cognition/persona.txt
@@ -76,7 +76,7 @@ Multi-agent pipeline: 12 specialist agents (memory, system, habit, self-improvem
 - Never prefix replies with "As ELI", "As ELI:", "ELI:", "Assistant:", or any similar role announcement. Speak directly.
 - Do not sound like HR, customer support, or a generic assistant. Avoid phrases like "I'd be pleased", "I'd be happy", "feel free to ask", "delve deeper", "a wealth of information", and "readily accessible" unless technically necessary.
 - Maintain ELI's working voice: direct, dry, technically grounded, sometimes darkly witty, but not performative.
-- Correct obvious user-name or STT mistakes contextually. If the user says "Jake" while the established user is Jason/Jay, treat it as likely "Jason" and say so plainly.
+- Correct obvious user-name or STT mistakes contextually. When a transcribed name doesn't match the established user identity from the runtime profile, treat it as a likely STT confusion of that established name and say so plainly.
 - Do not over-explain obvious profile paths. Use stored evidence, but translate it into meaningful identity/project context.
 - For follow-ups like "elaborate more", "go on", "continue", "more detail", use the immediately previous topic. Do not reject them as incomplete.
 - If a phrase is clearly truncated, say what was caught and ask for the missing part briefly; do not generate a full answer from scraps.
@@ -85,7 +85,7 @@ Multi-agent pipeline: 12 specialist agents (memory, system, habit, self-improvem
 # HARD PERSONA OVERRIDE — 2026-05-02
 You are ELI. Do not say "As ELI". Do not role-label yourself.
 No HR voice. No customer-support filler. No "I'd be happy", "I'd be pleased", "feel free", "delve deeper", or "wealth of information".
-Speak like the local system Jay built: direct, technically sharp, dry, and context-aware.
-Correct obvious user/STT mistakes bluntly but usefully. Example: if the user says "Jake" while the known user is Jason/Jay, respond: "Jake? I assume you mean Jason/Jay." Then answer the intended question.
+Speak like the local system you are: direct, technically sharp, dry, and context-aware.
+Correct obvious user/STT mistakes bluntly but usefully. When a transcribed name does not match the established user identity from the runtime profile, treat it as a likely STT confusion of that established name and surface the correction plainly before continuing.
 For "what's the story", "here we go", "what's going on", summarize the current operational situation, not random profile data.
 
diff --git a/eli/core/first_run.py b/eli/core/first_run.py
deleted file mode 100644
index 053ff5e..0000000
--- a/eli/core/first_run.py
+++ /dev/null
@@ -1,485 +0,0 @@
-# eli/core/first_run.py
-"""
-ELI First-Run Setup
-===================
-Runs once on first launch:
-  1. Checks if a GGUF model is already present
-  2. If not, asks user: use bundled model or download
-  3. Downloads and verifies if needed
-  4. Writes ~/.config/eli/config.json with model path
-
-Can be invoked headless (CLI) or with a Qt dialog.
-
-Environment variables:
-  ELI_MODEL_PATH       — override: skip all logic, use this model
-  ELI_MODEL_URL        — override default download URL
-  ELI_SKIP_FIRST_RUN   — set to 1 to skip entirely
-  ELI_BUNDLED_MODEL    — set to 1 to always use bundled model
-"""
-
-from __future__ import annotations
-
-import hashlib
-import json
-import os
-import shutil
-import sys
-import urllib.request
-from pathlib import Path
-from typing import Callable, Optional
-
-# ─────────────────────────────────────────────────────────────────────────────
-# Config
-# ─────────────────────────────────────────────────────────────────────────────
-
-# Default model to download (Mistral Small 3.1 Q4 — smaller/faster, good quality)
-DEFAULT_MODEL_URL = os.environ.get(
-    "ELI_MODEL_URL",
-    "https://huggingface.co/bartowski/mistral-small-3.1-24b-instruct-2503-GGUF"
-    "/resolve/main/mistral-small-3.1-24b-instruct-2503-Q4_K_M.gguf",
-)
-DEFAULT_MODEL_NAME = "mistral-small-3.1-24b-instruct-2503-Q4_K_M.gguf"
-DEFAULT_MODEL_SIZE_GB = 14.3
-
-# Bundled model path (relative to package — only present in bundled builds)
-_HERE = Path(__file__).resolve().parent.parent
-BUNDLED_MODEL_PATH = _HERE.parent / "models" / "gguf" / "base" / "mistral-small-3.1-24b-instruct-2503-q5_k_m.gguf"
-
-
-def _config_dir() -> Path:
-    d = Path(os.environ.get("ELI_CONFIG_DIR", "~/.config/eli")).expanduser()
-    d.mkdir(parents=True, exist_ok=True)
-    return d
-
-
-def _models_dir() -> Path:
-    d = Path(os.environ.get("ELI_MODELS_DIR", "models/gguf/base")).expanduser()
-    d.mkdir(parents=True, exist_ok=True)
-    return d
-
-
-def _config_path() -> Path:
-    return _config_dir() / "config.json"
-
-
-def _load_config() -> dict:
-    p = _config_path()
-    if p.exists():
-        try:
-            return json.loads(p.read_text())
-        except Exception:
-            pass
-    return {}
-
-
-def _save_config(cfg: dict) -> None:
-    _config_path().write_text(json.dumps(cfg, indent=2))
-
-
-# ─────────────────────────────────────────────────────────────────────────────
-# Model detection
-# ─────────────────────────────────────────────────────────────────────────────
-
-def find_existing_model() -> Optional[Path]:
-    """
-    Search for a GGUF model in priority order:
-      1. ELI_MODEL_PATH env var
-      2. config.json model_path
-      3. Bundled model (AppImage / pip --bundle)
-      4. models/gguf/base/
-      5. ~/Eli_OS_AGI/src/eli/models/ (dev layout)
-    """
-    # 1. Env override
-    env = os.environ.get("ELI_MODEL_PATH") or os.environ.get("ELI_GGUF_MODEL_PATH")
-    if env:
-        p = Path(env).expanduser()
-        if p.exists():
-            return p
-
-    # 2. Config
-    cfg = _load_config()
-    cfg_model = cfg.get("model_path") or cfg.get("gguf_model_path")
-    if cfg_model:
-        p = Path(cfg_model).expanduser()
-        if p.exists():
-            return p
-
-    # 3. Bundled
-    if BUNDLED_MODEL_PATH.exists():
-        return BUNDLED_MODEL_PATH
-
-    # 4. models/gguf/base/
-    for p in _models_dir().glob("*.gguf"):
-        return p  # return first found
-
-    # 5. Dev layout
-    dev_models = Path(os.environ.get("ELI_MODELS_DIR", "models/gguf/base")).expanduser().parent
-    if dev_models.exists():
-        for p in dev_models.glob("*.gguf"):
-            return p
-
-    return None
-
-
-def is_first_run() -> bool:
-    if os.environ.get("ELI_SKIP_FIRST_RUN") == "1":
-        return False
-    cfg = _load_config()
-    if cfg.get("first_run_complete"):
-        return False
-    if find_existing_model():
-        # Model exists — still mark complete so we don't ask again
-        _mark_complete(find_existing_model())
-        return False
-    return True
-
-
-def _mark_complete(model_path: Optional[Path]) -> None:
-    cfg = _load_config()
-    cfg["first_run_complete"] = True
-    if model_path:
-        cfg["model_path"] = str(model_path)
-    _save_config(cfg)
-
-
-# ─────────────────────────────────────────────────────────────────────────────
-# Download
-# ─────────────────────────────────────────────────────────────────────────────
-
-def download_model(
-    url: str = DEFAULT_MODEL_URL,
-    dest: Optional[Path] = None,
-    progress_cb: Optional[Callable[[int, int], None]] = None,
-) -> Path:
-    """
-    Download GGUF model with progress callback.
-
-    progress_cb(bytes_downloaded, total_bytes)
-    Raises RuntimeError on failure.
-    """
-    if dest is None:
-        dest = _models_dir() / DEFAULT_MODEL_NAME
-
-    dest.parent.mkdir(parents=True, exist_ok=True)
-    tmp = dest.with_suffix(".tmp")
-
-    print(f"[ELI] Downloading model to {dest}")
-    print(f"[ELI] URL: {url}")
-    print(f"[ELI] Size: ~{DEFAULT_MODEL_SIZE_GB:.1f} GB")
-
-    try:
-        req = urllib.request.Request(url, headers={"User-Agent": "ELI/1.0"})
-        with urllib.request.urlopen(req, timeout=30) as response:
-            total = int(response.headers.get("Content-Length", 0))
-            downloaded = 0
-            chunk = 1024 * 1024  # 1 MB chunks
-
-            with open(tmp, "wb") as f:
-                while True:
-                    data = response.read(chunk)
-                    if not data:
-                        break
-                    f.write(data)
-                    downloaded += len(data)
-                    if progress_cb:
-                        progress_cb(downloaded, total)
-                    if total:
-                        pct = downloaded / total * 100
-                        bar = "█" * int(pct / 2) + "░" * (50 - int(pct / 2))
-                        print(f"\r  [{bar}] {pct:.1f}%  {downloaded/1e9:.2f}/{total/1e9:.2f} GB", end="", flush=True)
-
-        print()  # newline after progress bar
-        tmp.rename(dest)
-        print(f"[ELI] ✅ Model downloaded: {dest}")
-        return dest
-
-    except Exception as exc:
-        if tmp.exists():
-            tmp.unlink()
-        raise RuntimeError(f"Model download failed: {exc}") from exc
-
-
-# ─────────────────────────────────────────────────────────────────────────────
-# CLI first-run flow
-# ─────────────────────────────────────────────────────────────────────────────
-
-def run_cli() -> Path:
-    """
-    Interactive CLI first-run wizard. Returns path to model file.
-    Called when no GUI is available.
-    """
-    print("=" * 60)
-    print("  ELI — First Run Setup")
-    print("=" * 60)
-    print()
-
-    # Check for bundled model
-    if BUNDLED_MODEL_PATH.exists():
-        print(f"✅ Bundled model found: {BUNDLED_MODEL_PATH}")
-        _mark_complete(BUNDLED_MODEL_PATH)
-        return BUNDLED_MODEL_PATH
-
-    print("No GGUF model found. ELI needs a language model to work.\n")
-    print("Options:")
-    print("  1) Download recommended model (~14 GB, Mistral Small 3.1 Q4)")
-    print("  2) Specify path to an existing GGUF file")
-    print("  3) Use Ollama instead (no download needed — ensure 'ollama serve' is running)")
-    print()
-
-    choice = input("Choice [1/2/3]: ").strip()
-
-    if choice == "1":
-        try:
-            model_path = download_model()
-            _mark_complete(model_path)
-            return model_path
-        except RuntimeError as e:
-            print(f"\n❌ {e}")
-            sys.exit(1)
-
-    elif choice == "2":
-        path_str = input("Enter full path to .gguf file: ").strip().strip('"')
-        p = Path(path_str).expanduser().resolve()
-        if not p.exists():
-            print(f"❌ File not found: {p}")
-            sys.exit(1)
-        _mark_complete(p)
-        return p
-
-    elif choice == "3":
-        cfg = _load_config()
-        cfg["inference_backend"] = "ollama"
-        cfg["first_run_complete"] = True
-        _save_config(cfg)
-        print("✅ Configured to use Ollama. Make sure 'ollama serve' is running.")
-        # Return empty path — caller should handle ollama mode
-        return Path("")
-
-    else:
-        print("Invalid choice.")
-        sys.exit(1)
-
-
-# ─────────────────────────────────────────────────────────────────────────────
-# Qt first-run dialog
-# ─────────────────────────────────────────────────────────────────────────────
-
-def run_qt_wizard() -> Optional[Path]:
-    """
-    Qt-based first-run wizard. Returns model path or None (Ollama mode).
-    Falls back to CLI if Qt is unavailable.
-    """
-    try:
-        from eli.gui.qt_compat import (
-            QApplication, QDialog, QVBoxLayout, QHBoxLayout,
-            QLabel, QPushButton, QProgressBar, QLineEdit,
-            QFileDialog, QRadioButton, QGroupBox, QMessageBox,
-            QFont,
-        )
-        from eli.gui.qt_compat import Qt, QThread, pyqtSignal
-    except ImportError:
-        return run_cli()
-
-    app = QApplication.instance() or QApplication(sys.argv)
-
-    class DownloadThread(QThread):
-        progress = pyqtSignal(int, int)
-        finished = pyqtSignal(str)
-        error = pyqtSignal(str)
-
-        def __init__(self, url: str, dest: Path):
-            super().__init__()
-            self.url = url
-            self.dest = dest
-
-        def run(self):
-            try:
-                path = download_model(
-                    url=self.url,
-                    dest=self.dest,
-                    progress_cb=lambda d, t: self.progress.emit(d, t),
-                )
-                self.finished.emit(str(path))
-            except Exception as exc:
-                self.error.emit(str(exc))
-
-    class FirstRunDialog(QDialog):
-        def __init__(self):
-            super().__init__()
-            self.model_path: Optional[Path] = None
-            self.download_thread: Optional[DownloadThread] = None
-            self._build_ui()
-
-        def _build_ui(self):
-            self.setWindowTitle("ELI — First Run Setup")
-            self.setMinimumWidth(520)
-            self.setWindowFlag(Qt.WindowStaysOnTopHint)
-
-            layout = QVBoxLayout(self)
-            layout.setSpacing(12)
-
-            title = QLabel("Welcome to ELI")
-            f = QFont(); f.setPointSize(16); f.setBold(True)
-            title.setFont(f)
-            layout.addWidget(title)
-
-            layout.addWidget(QLabel(
-                "ELI needs a language model. Choose how to set it up:"
-            ))
-
-            # Options
-            group = QGroupBox("Model source")
-            grp_layout = QVBoxLayout(group)
-
-            self.rb_download = QRadioButton(
-                f"Download recommended model  (~{DEFAULT_MODEL_SIZE_GB:.0f} GB, Mistral Small 3.1 Q4)"
-            )
-            self.rb_bundled = QRadioButton("Use bundled model  (already included)")
-            self.rb_existing = QRadioButton("Use existing GGUF file on disk")
-            self.rb_ollama = QRadioButton("Use Ollama  (no download — run 'ollama serve' separately)")
-
-            self.rb_download.setChecked(True)
-
-            if BUNDLED_MODEL_PATH.exists():
-                self.rb_bundled.setChecked(True)
-            else:
-                self.rb_bundled.setEnabled(False)
-
-            for rb in (self.rb_bundled, self.rb_download, self.rb_existing, self.rb_ollama):
-                grp_layout.addWidget(rb)
-
-            layout.addWidget(group)
-
-            # Existing file picker
-            pick_row = QHBoxLayout()
-            self.path_edit = QLineEdit()
-            self.path_edit.setPlaceholderText("Path to .gguf file…")
-            self.path_edit.setEnabled(False)
-            btn_browse = QPushButton("Browse…")
-            btn_browse.setFixedWidth(80)
-            btn_browse.clicked.connect(self._browse)
-            pick_row.addWidget(self.path_edit)
-            pick_row.addWidget(btn_browse)
-            layout.addLayout(pick_row)
-
-            self.rb_existing.toggled.connect(self.path_edit.setEnabled)
-            self.rb_existing.toggled.connect(btn_browse.setEnabled)
-
-            # Progress bar (hidden initially)
-            self.progress_bar = QProgressBar()
-            self.progress_bar.setVisible(False)
-            self.progress_label = QLabel("")
-            self.progress_label.setVisible(False)
-            layout.addWidget(self.progress_label)
-            layout.addWidget(self.progress_bar)
-
-            # Buttons
-            btn_row = QHBoxLayout()
-            self.btn_continue = QPushButton("Continue")
-            self.btn_continue.setDefault(True)
-            self.btn_continue.clicked.connect(self._on_continue)
-            btn_cancel = QPushButton("Cancel")
-            btn_cancel.clicked.connect(self.reject)
-            btn_row.addStretch()
-            btn_row.addWidget(btn_cancel)
-            btn_row.addWidget(self.btn_continue)
-            layout.addLayout(btn_row)
-
-        def _browse(self):
-            path, _ = QFileDialog.getOpenFileName(
-                self, "Select GGUF model", str(Path.home()), "GGUF files (*.gguf);;All files (*)"
-            )
-            if path:
-                self.path_edit.setText(path)
-
-        def _on_continue(self):
-            if self.rb_bundled.isChecked():
-                self.model_path = BUNDLED_MODEL_PATH
-                _mark_complete(self.model_path)
-                self.accept()
-
-            elif self.rb_existing.isChecked():
-                p = Path(self.path_edit.text().strip()).expanduser()
-                if not p.exists():
-                    QMessageBox.warning(self, "File not found", f"Cannot find:\n{p}")
-                    return
-                self.model_path = p
-                _mark_complete(self.model_path)
-                self.accept()
-
-            elif self.rb_ollama.isChecked():
-                cfg = _load_config()
-                cfg["inference_backend"] = "ollama"
-                cfg["first_run_complete"] = True
-                _save_config(cfg)
-                self.model_path = None
-                self.accept()
-
-            else:  # download
-                self._start_download()
-
-        def _start_download(self):
-            self.btn_continue.setEnabled(False)
-            self.progress_bar.setVisible(True)
-            self.progress_label.setVisible(True)
-            self.progress_label.setText("Starting download…")
-            dest = _models_dir() / DEFAULT_MODEL_NAME
-            self.download_thread = DownloadThread(DEFAULT_MODEL_URL, dest)
-            self.download_thread.progress.connect(self._on_progress)
-            self.download_thread.finished.connect(self._on_done)
-            self.download_thread.error.connect(self._on_error)
-            self.download_thread.start()
-
-        def _on_progress(self, downloaded: int, total: int):
-            if total:
-                self.progress_bar.setMaximum(total)
-                self.progress_bar.setValue(downloaded)
-                pct = downloaded / total * 100
-                self.progress_label.setText(
-                    f"Downloading… {downloaded/1e9:.2f} / {total/1e9:.2f} GB  ({pct:.0f}%)"
-                )
-
-        def _on_done(self, path_str: str):
-            self.model_path = Path(path_str)
-            _mark_complete(self.model_path)
-            self.accept()
-
-        def _on_error(self, msg: str):
-            self.btn_continue.setEnabled(True)
-            self.progress_bar.setVisible(False)
-            self.progress_label.setVisible(False)
-            QMessageBox.critical(self, "Download failed", msg)
-
-    dlg = FirstRunDialog()
-    if dlg.exec_() == QDialog.Accepted:
-        return dlg.model_path
-    return None
-
-
-# ─────────────────────────────────────────────────────────────────────────────
-# Public entry point
-# ─────────────────────────────────────────────────────────────────────────────
-
-def ensure_model(gui: bool = True) -> Optional[Path]:
-    """
-    Call at ELI startup. Returns model path, or None if Ollama mode.
-    Raises SystemExit if user cancels.
-    """
-    if os.environ.get("ELI_SKIP_FIRST_RUN") == "1":
-        return find_existing_model()
-
-    existing = find_existing_model()
-    if existing:
-        return existing
-
-    if gui:
-        return run_qt_wizard()
-    return run_cli()
-
-
-if __name__ == "__main__":
-    path = ensure_model(gui="--cli" not in sys.argv)
-    if path:
-        print(f"Model ready: {path}")
-    else:
-        print("Ollama mode — no local model required.")
diff --git a/eli/core/first_run_wizard.py b/eli/core/first_run_wizard.py
deleted file mode 100644
index 432c45b..0000000
--- a/eli/core/first_run_wizard.py
+++ /dev/null
@@ -1,733 +0,0 @@
-"""
-ELI First-Run Bootstrap Wizard
-================================
-PySide6 GUI wizard for new installations.
-Handles: directory creation, model acquisition, schema init, config generation.
-
-Usage:
-    - Called automatically by gui/app.py on first launch
-    - Can be run standalone: python -m core.first_run_wizard
-    - CLI fallback: python -m core.first_run_wizard --cli
-"""
-
-import os
-import sys
-import json
-import sqlite3
-import logging
-import threading
-from pathlib import Path
-from typing import Optional
-
-log = logging.getLogger(__name__)
-
-# ── First-run detection ──
-
-def is_first_run() -> bool:
-    """Check if this is a first run (no config or no user DB)."""
-    try:
-        from eli.core.paths import config_dir, user_db_path
-        sentinel = config_dir() / ".eli_initialized"
-        if sentinel.exists():
-            return False
-        if user_db_path().exists():
-            return False
-        return True
-    except Exception:
-        return True
-
-
-def mark_initialized():
-    """Write the first-run sentinel file."""
-    from eli.core.paths import config_dir
-    sentinel = config_dir() / ".eli_initialized"
-    sentinel.parent.mkdir(parents=True, exist_ok=True)
-    sentinel.write_text(f"initialized=true\n")
-
-
-# ── Schema initialization ──
-
-USER_SCHEMA = """
-CREATE TABLE IF NOT EXISTS conversation_turns (
-    id INTEGER PRIMARY KEY AUTOINCREMENT,
-    timestamp TEXT DEFAULT (datetime('now')),
-    role TEXT NOT NULL,
-    content TEXT NOT NULL,
-    session_id TEXT,
-    metadata TEXT
-);
-
-CREATE TABLE IF NOT EXISTS memories (
-    id INTEGER PRIMARY KEY AUTOINCREMENT,
-    timestamp TEXT DEFAULT (datetime('now')),
-    category TEXT NOT NULL DEFAULT 'general',
-    key TEXT,
-    value TEXT NOT NULL,
-    confidence REAL DEFAULT 0.5,
-    source TEXT DEFAULT 'user',
-    metadata TEXT
-);
-
-CREATE TABLE IF NOT EXISTS semantic (
-    id INTEGER PRIMARY KEY AUTOINCREMENT,
-    timestamp TEXT DEFAULT (datetime('now')),
-    content TEXT NOT NULL,
-    embedding BLOB,
-    category TEXT DEFAULT 'general',
-    metadata TEXT
-);
-
-CREATE TABLE IF NOT EXISTS procedural (
-    id INTEGER PRIMARY KEY AUTOINCREMENT,
-    timestamp TEXT DEFAULT (datetime('now')),
-    name TEXT NOT NULL,
-    steps TEXT NOT NULL,
-    metadata TEXT
-);
-
-CREATE TABLE IF NOT EXISTS self_model (
-    id INTEGER PRIMARY KEY AUTOINCREMENT,
-    timestamp TEXT DEFAULT (datetime('now')),
-    attribute TEXT NOT NULL,
-    value TEXT NOT NULL,
-    confidence REAL DEFAULT 0.5
-);
-
-CREATE TABLE IF NOT EXISTS world_state (
-    id INTEGER PRIMARY KEY AUTOINCREMENT,
-    timestamp TEXT DEFAULT (datetime('now')),
-    key TEXT NOT NULL,
-    value TEXT NOT NULL,
-    source TEXT DEFAULT 'observation'
-);
-
-CREATE TABLE IF NOT EXISTS session_summaries (
-    id INTEGER PRIMARY KEY AUTOINCREMENT,
-    session_id TEXT NOT NULL,
-    timestamp TEXT DEFAULT (datetime('now')),
-    summary TEXT NOT NULL,
-    turn_count INTEGER DEFAULT 0
-);
-
-CREATE TABLE IF NOT EXISTS observations (
-    id INTEGER PRIMARY KEY AUTOINCREMENT,
-    timestamp TEXT DEFAULT (datetime('now')),
-    category TEXT NOT NULL DEFAULT 'general',
-    content TEXT NOT NULL,
-    source TEXT DEFAULT 'system',
-    metadata TEXT
-);
-"""
-
-AGENT_SCHEMA = """
-CREATE TABLE IF NOT EXISTS agent_state (
-    id INTEGER PRIMARY KEY AUTOINCREMENT,
-    agent_name TEXT NOT NULL,
-    timestamp TEXT DEFAULT (datetime('now')),
-    state_json TEXT NOT NULL
-);
-
-CREATE TABLE IF NOT EXISTS agent_results (
-    id INTEGER PRIMARY KEY AUTOINCREMENT,
-    timestamp TEXT DEFAULT (datetime('now')),
-    action TEXT NOT NULL,
-    agent_name TEXT NOT NULL,
-    result_json TEXT NOT NULL,
-    elapsed_ms REAL
-);
-"""
-
-
-def init_schema(db_path: Path, schema: str):
-    """Initialize a SQLite database with the given schema."""
-    db_path.parent.mkdir(parents=True, exist_ok=True)
-    conn = sqlite3.connect(str(db_path))
-    conn.executescript(schema)
-    conn.execute("PRAGMA journal_mode=WAL")
-    conn.close()
-    log.info(f"Initialized schema: {db_path}")
-
-
-def init_all_schemas():
-    """Initialize all database schemas."""
-    from eli.core.paths import user_db_path, agent_db_path
-    init_schema(user_db_path(), USER_SCHEMA)
-    init_schema(agent_db_path(), AGENT_SCHEMA)
-
-
-# ── Default config generation ──
-
-def generate_default_config():
-    """Generate default config files."""
-    from eli.core.paths import config_dir
-    
-    settings = config_dir() / "settings.json"
-    if not settings.exists():
-        settings.parent.mkdir(parents=True, exist_ok=True)
-        default = {
-            "inference": {
-                "ctx_size": 8192,
-                "gpu_layers": "auto",
-                "batch_size": 512,
-                "threads": "auto"
-            },
-            "voice": {
-                "enabled": False,
-                "tts_engine": "piper",
-                "stt_engine": "whisper"
-            },
-            "gui": {
-                "theme": "dark",
-                "font_size": 12,
-                "show_system_tray": True
-            },
-            "proactive": {
-                "enabled": True,
-                "interval_minutes": 30
-            }
-        }
-        settings.write_text(json.dumps(default, indent=2))
-        log.info(f"Created default config: {settings}")
-
-    user_id = config_dir() / "user_id"
-    if not user_id.exists():
-        import uuid
-        user_id.write_text(str(uuid.uuid4()))
-
-
-# ── Model discovery ──
-
-def find_local_models() -> list[dict]:
-    """Find GGUF models in the models directory."""
-    from eli.core.paths import models_dir
-    mdir = models_dir()
-    models = []
-    if mdir.exists():
-        for f in sorted(mdir.glob("*.gguf")):
-            size_gb = f.stat().st_size / (1024 ** 3)
-            models.append({
-                "path": str(f),
-                "name": f.stem,
-                "size": f.stat().st_size,
-                "size_str": f"{size_gb:.1f}G" if size_gb >= 1 else f"{f.stat().st_size // (1024*1024)}M",
-            })
-    return models
-
-
-# ── Model download ──
-
-DEFAULT_MODEL = {
-    "repo_id": "bartowski/Qwen2.5-7B-Instruct-GGUF",
-    "filename": "Qwen2.5-7B-Instruct-Q4_K_M.gguf",
-    "display_name": "Qwen2.5-7B-Instruct (Q4_K_M, ~4.4GB)",
-    "size_bytes": 4_700_000_000,
-}
-
-AVAILABLE_MODELS = [
-    DEFAULT_MODEL,
-    {
-        "repo_id": "Qwen/Qwen2.5-3B-Instruct-GGUF",
-        "filename": "qwen2.5-3b-instruct-q4_k_m.gguf",
-        "display_name": "Qwen2.5-3B-Instruct (Q4_K_M, ~2.0GB)",
-        "size_bytes": 2_000_000_000,
-    },
-    {
-        "repo_id": "bartowski/Phi-3.5-mini-instruct-GGUF",
-        "filename": "Phi-3.5-mini-instruct-Q4_K_M.gguf",
-        "display_name": "Phi-3.5-mini-Instruct (Q4_K_M, ~2.2GB)",
-        "size_bytes": 2_000_000_000,
-    },
-    {
-        "repo_id": "Writer/Palmyra-mini-GGUF",
-        "filename": "Palmyra-mini-Q8_0.gguf",
-        "display_name": "Palmyra-mini (Q8_0, ~1.9GB) — high precision",
-        "size_bytes": 1_900_000_000,
-    },
-    {
-        "repo_id": "second-state/StarCoder2-3B-GGUF",
-        "filename": "starcoder2-3b-Q4_K_M.gguf",
-        "display_name": "StarCoder2-3B (Q4_K_M, ~1.8GB) — code specialist",
-        "size_bytes": 1_800_000_000,
-    },
-    {
-        "repo_id": "bartowski/SmolLM2-1.7B-Instruct-GGUF",
-        "filename": "SmolLM2-1.7B-Instruct-Q4_K_M.gguf",
-        "display_name": "SmolLM2-1.7B (Q4_K_M, ~1GB) — minimal",
-        "size_bytes": 1_000_000_000,
-    },
-]
-
-
-def download_model(repo_id: str, filename: str, dest_dir: Path,
-                   progress_callback=None) -> Path:
-    """
-    Download a GGUF model from Hugging Face Hub.
-    progress_callback(bytes_downloaded, total_bytes) is called periodically.
-    """
-    try:
-        from huggingface_hub import hf_hub_download
-    except ImportError:
-        raise RuntimeError(
-            "huggingface_hub not installed. Install with: pip install huggingface-hub"
-        )
-
-    dest_dir.mkdir(parents=True, exist_ok=True)
-    dest = dest_dir / filename
-
-    if dest.exists():
-        log.info(f"Model already exists: {dest}")
-        return dest
-
-    log.info(f"Downloading {repo_id}/{filename} to {dest_dir}...")
-
-    # hf_hub_download handles caching and resume
-    downloaded = hf_hub_download(
-        repo_id=repo_id,
-        filename=filename,
-        local_dir=str(dest_dir),
-        local_dir_use_symlinks=False,
-    )
-
-    return Path(downloaded)
-
-
-# ══════════════════════════════════════════════════════════════
-# CLI BOOTSTRAP
-# ══════════════════════════════════════════════════════════════
-
-def run_cli_bootstrap():
-    """Text-based first-run setup for headless installs."""
-    from eli.core.paths import ensure_dirs, models_dir, path_info
-
-    print("\n" + "=" * 55)
-    print("  ELI — First-Run Setup")
-    print("=" * 55)
-
-    # Step 1: Create directories
-    print("\n[1/4] Creating directories...")
-    ensure_dirs()
-    info = path_info()
-    print(f"  Data:   {info['data_dir']}")
-    print(f"  Config: {info['config_dir']}")
-    print(f"  Models: {info['models_dir']}")
-    print("  ✅ Directories created")
-
-    # Step 2: Generate config
-    print("\n[2/4] Generating default configuration...")
-    generate_default_config()
-    print("  ✅ Config created")
-
-    # Step 3: Initialize databases
-    print("\n[3/4] Initializing databases...")
-    init_all_schemas()
-    print("  ✅ Schemas initialized")
-
-    # Step 4: Model selection
-    print("\n[4/4] Model setup...")
-    local = find_local_models()
-    if local:
-        print(f"  Found {len(local)} local model(s):")
-        for i, m in enumerate(local, 1):
-            print(f"    {i}. {m['name']}  ({m['size_str']})")
-        print(f"    {len(local) + 1}. Download a new model")
-        print(f"    {len(local) + 2}. Browse for a .gguf file")
-
-        choice = input(f"\n  Choose [1]: ").strip() or "1"
-        try:
-            idx = int(choice) - 1
-            if 0 <= idx < len(local):
-                model_path = local[idx]["path"]
-            elif idx == len(local):
-                model_path = _cli_download_model()
-            else:
-                model_path = _cli_browse_model()
-        except (ValueError, IndexError):
-            model_path = local[0]["path"]
-    else:
-        print("  No local models found.")
-        print("  1. Download Qwen2.5-7B-Instruct (~4.4GB)")
-        print("  2. Download Qwen2.5-3B-Instruct (~2.0GB)")
-        print("  3. Download SmolLM2-1.7B (~1GB)")
-        print("  4. Browse for existing .gguf file")
-
-        choice = input("\n  Choose [1]: ").strip() or "1"
-        if choice == "4":
-            model_path = _cli_browse_model()
-        else:
-            idx = int(choice) - 1 if choice.isdigit() else 0
-            idx = max(0, min(idx, len(AVAILABLE_MODELS) - 1))
-            model_path = _cli_download_model(idx)
-
-    if model_path:
-        from eli.core.paths import set_gguf_model_path
-        set_gguf_model_path(str(model_path))
-        print(f"\n  ✅ Model set: {Path(model_path).name}")
-
-    mark_initialized()
-    print("\n" + "=" * 55)
-    print("  Setup complete! Run 'eli' to start.")
-    print("=" * 55 + "\n")
-
-
-def _cli_download_model(model_idx: int = 0) -> Optional[str]:
-    """Download a model via CLI with progress."""
-    from eli.core.paths import models_dir
-    model = AVAILABLE_MODELS[model_idx]
-    print(f"\n  Downloading {model['display_name']}...")
-    print(f"  This may take a few minutes...\n")
-
-    try:
-        path = download_model(model["repo_id"], model["filename"], models_dir())
-        return str(path)
-    except Exception as e:
-        print(f"  ❌ Download failed: {e}")
-        print("  You can manually place a .gguf file in:", models_dir())
-        return None
-
-
-def _cli_browse_model() -> Optional[str]:
-    """Let user type a path to a .gguf file."""
-    path = input("  Enter path to .gguf file: ").strip()
-    if path and Path(path).expanduser().exists():
-        return str(Path(path).expanduser().resolve())
-    print("  ❌ File not found")
-    return None
-
-
-# ══════════════════════════════════════════════════════════════
-# PySide6 GUI WIZARD
-# ══════════════════════════════════════════════════════════════
-
-def run_gui_wizard() -> bool:
-    """
-    Run the PySide6 first-run wizard.
-    Returns True if setup completed, False if cancelled.
-    """
-    try:
-        from PySide6.QtWidgets import (
-            QWizard, QWizardPage, QVBoxLayout, QHBoxLayout,
-            QLabel, QPushButton, QProgressBar, QRadioButton,
-            QButtonGroup, QFileDialog, QApplication, QGroupBox,
-            QTextEdit, QSizePolicy,
-        )
-        from PySide6.QtCore import Qt, QThread, Signal
-        from PySide6.QtGui import QFont
-    except ImportError:
-        log.warning("PySide6 not available, falling back to CLI")
-        run_cli_bootstrap()
-        return True
-
-    from eli.core.paths import ensure_dirs, models_dir, path_info, set_gguf_model_path
-
-    # ── Download worker thread ──
-    class DownloadWorker(QThread):
-        progress = Signal(int, int)   # bytes_done, bytes_total
-        finished = Signal(str)        # path or empty on error
-        error = Signal(str)
-
-        def __init__(self, repo_id, filename, dest_dir):
-            super().__init__()
-            self.repo_id = repo_id
-            self.filename = filename
-            self.dest_dir = dest_dir
-
-        def run(self):
-            try:
-                path = download_model(self.repo_id, self.filename, self.dest_dir)
-                self.finished.emit(str(path))
-            except Exception as e:
-                self.error.emit(str(e))
-
-    # ── Page 1: Welcome ──
-    class WelcomePage(QWizardPage):
-        def __init__(self):
-            super().__init__()
-            self.setTitle("Welcome to ELI")
-            self.setSubTitle("Enhanced Learning Interface — First-Time Setup")
-
-            layout = QVBoxLayout(self)
-            layout.setSpacing(16)
-
-            welcome = QLabel(
-                "ELI is a fully offline AI personal assistant that runs on your machine.\n\n"
-                "This wizard will:\n"
-                "  • Create data directories\n"
-                "  • Set up databases\n"
-                "  • Configure a language model\n"
-                "  • Generate default settings\n\n"
-                "Everything stays local — no data leaves your computer."
-            )
-            welcome.setWordWrap(True)
-            welcome.setFont(QFont("sans-serif", 11))
-            layout.addWidget(welcome)
-
-            # Show resolved paths
-            info = path_info()
-            paths_box = QGroupBox("Installation Paths")
-            paths_layout = QVBoxLayout(paths_box)
-            for label, key in [("Data", "data_dir"), ("Config", "config_dir"),
-                               ("Models", "models_dir"), ("Logs", "logs_dir")]:
-                paths_layout.addWidget(QLabel(f"  {label}: {info[key]}"))
-            layout.addWidget(paths_box)
-            layout.addStretch()
-
-    # ── Page 2: Model Selection ──
-    class ModelPage(QWizardPage):
-        def __init__(self):
-            super().__init__()
-            self.setTitle("Choose a Language Model")
-            self.setSubTitle("Select an existing model or download one")
-            self.model_path = None
-
-            layout = QVBoxLayout(self)
-            layout.setSpacing(12)
-
-            self.btn_group = QButtonGroup(self)
-            self.radios = []
-
-            # Local models
-            local = find_local_models()
-            if local:
-                grp = QGroupBox("Local Models Found")
-                grp_layout = QVBoxLayout(grp)
-                for i, m in enumerate(local):
-                    rb = QRadioButton(f"{m['name']}  ({m['size_str']})")
-                    rb.setProperty("model_path", m["path"])
-                    self.btn_group.addButton(rb, i)
-                    self.radios.append(rb)
-                    grp_layout.addWidget(rb)
-                    if i == 0:
-                        rb.setChecked(True)
-                        self.model_path = m["path"]
-                layout.addWidget(grp)
-
-            # Download options
-            dl_grp = QGroupBox("Download a Model")
-            dl_layout = QVBoxLayout(dl_grp)
-            base_id = len(local) if local else 0
-            for i, m in enumerate(AVAILABLE_MODELS):
-                rb = QRadioButton(m["display_name"])
-                rb.setProperty("download_idx", i)
-                self.btn_group.addButton(rb, base_id + i)
-                self.radios.append(rb)
-                dl_layout.addWidget(rb)
-                if not local and i == 0:
-                    rb.setChecked(True)
-            layout.addWidget(dl_grp)
-
-            # Browse button
-            browse_btn = QPushButton("Browse for .gguf file...")
-            browse_btn.clicked.connect(self._browse)
-            layout.addWidget(browse_btn)
-
-            self.status_label = QLabel("")
-            layout.addWidget(self.status_label)
-            layout.addStretch()
-
-            self.btn_group.buttonClicked.connect(self._on_select)
-
-        def _on_select(self, btn):
-            path = btn.property("model_path")
-            if path:
-                self.model_path = path
-                self.status_label.setText(f"Selected: {Path(path).name}")
-
-        def _browse(self):
-            path, _ = QFileDialog.getOpenFileName(
-                self, "Select GGUF Model", str(Path.home()),
-                "GGUF Models (*.gguf);;All Files (*)"
-            )
-            if path:
-                self.model_path = path
-                self.status_label.setText(f"Selected: {Path(path).name}")
-                # Uncheck radio buttons
-                checked = self.btn_group.checkedButton()
-                if checked:
-                    self.btn_group.setExclusive(False)
-                    checked.setChecked(False)
-                    self.btn_group.setExclusive(True)
-
-        def get_download_info(self) -> Optional[dict]:
-            """If a download option is selected, return its info."""
-            checked = self.btn_group.checkedButton()
-            if checked:
-                idx = checked.property("download_idx")
-                if idx is not None:
-                    return AVAILABLE_MODELS[idx]
-            return None
-
-    # ── Page 3: Download Progress ──
-    class DownloadPage(QWizardPage):
-        def __init__(self, model_page: ModelPage):
-            super().__init__()
-            self.setTitle("Downloading Model")
-            self.setSubTitle("Please wait while the model downloads...")
-            self.model_page = model_page
-            self.complete = False
-            self.worker = None
-
-            layout = QVBoxLayout(self)
-            layout.setSpacing(16)
-
-            self.progress_bar = QProgressBar()
-            self.progress_bar.setMinimum(0)
-            self.progress_bar.setMaximum(100)
-            layout.addWidget(self.progress_bar)
-
-            self.status = QLabel("Preparing download...")
-            self.status.setWordWrap(True)
-            layout.addWidget(self.status)
-
-            self.log_area = QTextEdit()
-            self.log_area.setReadOnly(True)
-            self.log_area.setMaximumHeight(120)
-            layout.addWidget(self.log_area)
-            layout.addStretch()
-
-        def initializePage(self):
-            dl = self.model_page.get_download_info()
-            if dl is None:
-                # No download needed — local model selected
-                self.status.setText("Using local model — no download needed.")
-                self.progress_bar.setValue(100)
-                self.complete = True
-                self.completeChanged.emit()
-                return
-
-            self.status.setText(f"Downloading {dl['display_name']}...")
-            self.progress_bar.setValue(0)
-
-            self.worker = DownloadWorker(
-                dl["repo_id"], dl["filename"], models_dir()
-            )
-            self.worker.progress.connect(self._on_progress)
-            self.worker.finished.connect(self._on_finished)
-            self.worker.error.connect(self._on_error)
-            self.worker.start()
-
-        def _on_progress(self, done, total):
-            if total > 0:
-                pct = int(done / total * 100)
-                self.progress_bar.setValue(pct)
-                done_mb = done // (1024 * 1024)
-                total_mb = total // (1024 * 1024)
-                self.status.setText(f"Downloading... {done_mb}MB / {total_mb}MB ({pct}%)")
-
-        def _on_finished(self, path):
-            self.model_page.model_path = path
-            self.progress_bar.setValue(100)
-            self.status.setText(f"✅ Download complete: {Path(path).name}")
-            self.complete = True
-            self.completeChanged.emit()
-
-        def _on_error(self, msg):
-            self.status.setText(f"❌ Download failed: {msg}")
-            self.log_area.append(f"Error: {msg}")
-            self.log_area.append("You can manually place a .gguf file in the MKXI models/gguf/base directory.")
-            self.complete = True  # Allow proceeding without model
-            self.completeChanged.emit()
-
-        def isComplete(self):
-            return self.complete
-
-    # ── Page 4: Finish ──
-    class FinishPage(QWizardPage):
-        def __init__(self, model_page: ModelPage):
-            super().__init__()
-            self.setTitle("Setup Complete")
-            self.setSubTitle("ELI is ready to use")
-            self.model_page = model_page
-
-            layout = QVBoxLayout(self)
-            layout.setSpacing(16)
-
-            self.summary = QLabel("")
-            self.summary.setWordWrap(True)
-            self.summary.setFont(QFont("sans-serif", 11))
-            layout.addWidget(self.summary)
-            layout.addStretch()
-
-        def initializePage(self):
-            # Perform final setup
-            ensure_dirs()
-            generate_default_config()
-            init_all_schemas()
-
-            model = self.model_page.model_path
-            if model:
-                set_gguf_model_path(str(model))
-
-            mark_initialized()
-
-            info = path_info()
-            model_name = Path(model).name if model else "None selected"
-            self.summary.setText(
-                f"✅ Directories created\n"
-                f"✅ Databases initialized\n"
-                f"✅ Configuration generated\n"
-                f"✅ Model: {model_name}\n\n"
-                f"Data directory: {info['data_dir']}\n"
-                f"Config directory: {info['config_dir']}\n\n"
-                f"Click Finish to launch ELI."
-            )
-
-    # ── Build and run the wizard ──
-    app = QApplication.instance()
-    standalone = app is None
-    if standalone:
-        app = QApplication(sys.argv)
-
-    wizard = QWizard()
-    wizard.setWindowTitle("ELI — First-Time Setup")
-    wizard.setMinimumSize(600, 480)
-    wizard.setWizardStyle(QWizard.ModernStyle)
-
-    model_page = ModelPage()
-
-    wizard.addPage(WelcomePage())
-    wizard.addPage(model_page)
-    wizard.addPage(DownloadPage(model_page))
-    wizard.addPage(FinishPage(model_page))
-
-    result = wizard.exec()
-
-    if standalone:
-        app.quit()
-
-    return result == QWizard.Accepted
-
-
-# ══════════════════════════════════════════════════════════════
-# PUBLIC API
-# ══════════════════════════════════════════════════════════════
-
-def run_bootstrap(force_cli: bool = False) -> bool:
-    """
-    Run first-run bootstrap if needed.
-    Returns True if setup was completed (or wasn't needed).
-    """
-    if not is_first_run():
-        return True
-
-    if force_cli or "--cli" in sys.argv or not _has_display():
-        run_cli_bootstrap()
-        return True
-    else:
-        return run_gui_wizard()
-
-
-def _has_display() -> bool:
-    """Check if a display is available (for deciding GUI vs CLI)."""
-    if sys.platform == "win32":
-        return True
-    if sys.platform == "darwin":
-        return True
-    return os.environ.get("DISPLAY") or os.environ.get("WAYLAND_DISPLAY")
-
-
-if __name__ == "__main__":
-    logging.basicConfig(level=logging.INFO)
-    if "--cli" in sys.argv:
-        run_cli_bootstrap()
-    else:
-        run_gui_wizard()
diff --git a/eli/execution/media_runtime.py b/eli/execution/media_runtime.py
index 8175557..5a907ab 100644
--- a/eli/execution/media_runtime.py
+++ b/eli/execution/media_runtime.py
@@ -10,7 +10,6 @@ Owns:
 This module deliberately does not own STT, wake words, routing, or GUI logic.
 """
 
-from __future__ import annotations
 
 import json
 import os
diff --git a/eli/kernel/engine.py b/eli/kernel/engine.py
index 5f51cf3..0582689 100644
--- a/eli/kernel/engine.py
+++ b/eli/kernel/engine.py
@@ -3056,6 +3056,38 @@ Answer:"""
      threshold=threshold)
         best_answer = ''
         best_score = 0.0
+
+        # Algorithmic-mode dispatch: ToT / Constitutional / Self-Consistency
+        # run a multi-stage pipeline of LLM calls (propose-and-develop,
+        # generate-critique-revise, sample-and-select) instead of just
+        # repeating the same single call N times. The pass-loop below still
+        # acts as a confidence safety net if the algorithmic output scores
+        # too low.
+        _mode_str = str(profile.get('mode') or reasoning_mode or '').strip().lower()
+        if self._supports_mode_algorithm(_mode_str) and not _eli_test_mode():
+            try:
+                algo_resp = self._run_mode_algorithm(
+                    _mode_str, user_input, working_context, gen_overrides, situation_brief
+                )
+            except Exception as exc:
+                print(f"[REASONING][{_mode_str}] algorithm failed: {exc} — falling through to pass-loop")
+                algo_resp = None
+            if algo_resp:
+                algo_score = self._score_response_confidence(
+                    user_input, algo_resp, working_context, intent_conf, evidence
+                )
+                print(f"[REASONING][{_mode_str}] final score={algo_score:.2f} threshold={threshold:.2f}")
+                if trace is not None:
+                    trace.setdefault('confidence', []).append(
+                        {'pass_no': 'algorithm', 'score': algo_score, 'threshold': threshold})
+                if algo_score >= threshold:
+                    return {'response': algo_resp, 'score': algo_score,
+                            'threshold': threshold, 'evidence': evidence, 'clarified': False}
+                # Below threshold — seed the pass-loop with the algorithmic
+                # output as best-so-far so retries refine from it.
+                best_answer = algo_resp
+                best_score = algo_score
+
         for pass_no in range(1, passes + 1):
             if trace is not None:
                 self._trace_phase(
@@ -3115,6 +3147,164 @@ Answer:"""
         return {'response': best_answer, 'score': best_score,
             'threshold': threshold, 'evidence': evidence, 'clarified': False}
 
+    # ─────────────────────────────────────────────────────────────────────────
+    # Reasoning-mode algorithms.
+    #
+    # Modes are not just system-prompt strings on the same single-pass call.
+    # Each one runs its own algorithm:
+    #   - tree_of_thoughts:  propose K candidate approaches → score → develop best
+    #   - constitutional_ai: generate → critique against principles → revise
+    #   - self_consistency:  N samples → LLM picks the most consistent
+    #   - chain_of_thought:  single call (instruction enforces step format)
+    #   - quick:             single call, no overhead
+    # The cheaper modes route through the standard pass-loop above; the
+    # heavier modes call the helpers below. All paths still feed the
+    # confidence-threshold retry as a safety net.
+    # ─────────────────────────────────────────────────────────────────────────
+
+    def _run_tree_of_thoughts(self, user_input: str, working_context: str,
+                              gen_overrides: Dict[str, Any], situation_brief: str,
+                              k: int = 3) -> str:
+        """Branch-and-develop: propose K approaches, score, develop the best."""
+        propose_prompt = (
+            f"For the following request, propose {k} DISTINCT high-level approaches. "
+            f"For each: name it (1-5 words), state the core idea (1 sentence), "
+            f"and rate feasibility 1-10 with one-line justification. "
+            f"Output as a numbered list. Do NOT solve the problem yet — just enumerate paths.\n\n"
+            f"REQUEST: {user_input}"
+        )
+        propose_overrides = dict(gen_overrides or {})
+        propose_overrides["temperature"] = 0.6
+        propose_overrides["max_tokens"] = 600
+        candidates = self._get_chat_response(
+            propose_prompt, working_context,
+            reasoning_mode="tree_of_thoughts", gen_overrides=propose_overrides,
+            situation_brief=situation_brief,
+        )
+        print(f"[REASONING][ToT] proposed {k} candidates ({len(candidates)} chars)")
+
+        develop_prompt = (
+            f"You previously enumerated {k} approaches:\n\n{candidates}\n\n"
+            f"Pick the highest-scoring approach (break ties by feasibility, then by clarity). "
+            f"Develop ONLY that approach into a complete answer to the original request. "
+            f"Do not narrate the selection; the user wants the answer, not the meta.\n\n"
+            f"ORIGINAL REQUEST: {user_input}"
+        )
+        develop_overrides = dict(gen_overrides or {})
+        develop_overrides["temperature"] = float(gen_overrides.get("temperature", 0.4))
+        final = self._get_chat_response(
+            develop_prompt, working_context,
+            reasoning_mode="tree_of_thoughts", gen_overrides=develop_overrides,
+            situation_brief=situation_brief,
+        )
+        print(f"[REASONING][ToT] developed final ({len(final)} chars)")
+        return final
+
+    def _run_constitutional_ai(self, user_input: str, working_context: str,
+                               gen_overrides: Dict[str, Any], situation_brief: str) -> str:
+        """Generate → critique against principles → revise."""
+        # Stage 1: initial response
+        gen_overrides_1 = dict(gen_overrides or {})
+        initial = self._get_chat_response(
+            user_input, working_context,
+            reasoning_mode="constitutional_ai", gen_overrides=gen_overrides_1,
+            situation_brief=situation_brief,
+        )
+        print(f"[REASONING][Constitutional] initial draft ({len(initial)} chars)")
+
+        # Stage 2: critique against principles
+        critique_prompt = (
+            "Critique the following draft response against these principles. "
+            "For each, output PASS or FAIL with one-sentence justification:\n"
+            "  P1. Factual accuracy: every claim is grounded in supplied context or general knowledge known to be reliable.\n"
+            "  P2. No unsupported claims: nothing asserted that the draft cannot defend.\n"
+            "  P3. Completeness: the draft answers what was actually asked, not a related question.\n"
+            "  P4. Honesty about uncertainty: if something is unknown, the draft says so.\n"
+            "  P5. No harm: the draft does not recommend actions that would cause harm if followed.\n\n"
+            f"REQUEST: {user_input}\n\nDRAFT:\n{initial}\n\n"
+            f"Output exactly:\nP1: PASS|FAIL — reason\nP2: PASS|FAIL — reason\n... etc.\n"
+            f"After the five lines, list the two most important specific revisions needed."
+        )
+        critique_overrides = dict(gen_overrides or {})
+        critique_overrides["temperature"] = 0.2
+        critique_overrides["max_tokens"] = 500
+        critique = self._get_chat_response(
+            critique_prompt, working_context,
+            reasoning_mode="constitutional_ai", gen_overrides=critique_overrides,
+            situation_brief=situation_brief,
+        )
+        print(f"[REASONING][Constitutional] critique ({len(critique)} chars)")
+
+        # Stage 3: revise. Skip stage 3 if all five principles passed (fast path).
+        all_pass = all(f"P{i}: PASS" in critique.upper().replace(" ", " ") for i in range(1, 6))
+        if all_pass:
+            print("[REASONING][Constitutional] all principles PASS → returning initial draft")
+            return initial
+
+        revise_prompt = (
+            "Revise the draft to address the critique. Output ONLY the revised response — "
+            "do not narrate the revision, do not restate the critique.\n\n"
+            f"REQUEST: {user_input}\n\nORIGINAL DRAFT:\n{initial}\n\nCRITIQUE:\n{critique}"
+        )
+        revise_overrides = dict(gen_overrides or {})
+        final = self._get_chat_response(
+            revise_prompt, working_context,
+            reasoning_mode="constitutional_ai", gen_overrides=revise_overrides,
+            situation_brief=situation_brief,
+        )
+        print(f"[REASONING][Constitutional] revised final ({len(final)} chars)")
+        return final
+
+    def _run_self_consistency(self, user_input: str, working_context: str,
+                              gen_overrides: Dict[str, Any], situation_brief: str,
+                              n: int = 3) -> str:
+        """Sample N independent answers, then LLM-pick the most consistent."""
+        samples: list = []
+        sample_overrides = dict(gen_overrides or {})
+        sample_overrides["temperature"] = max(float(sample_overrides.get("temperature", 0.6)), 0.7)
+        for i in range(n):
+            s = self._get_chat_response(
+                user_input, working_context,
+                reasoning_mode="self_consistency", gen_overrides=sample_overrides,
+                situation_brief=situation_brief,
+            )
+            samples.append(s)
+            print(f"[REASONING][SelfConsistency] sample {i+1}/{n} ({len(s)} chars)")
+
+        # Single selection call: which sample is most defensible / consistent?
+        labelled = "\n\n".join(f"=== SAMPLE {i+1} ===\n{s}" for i, s in enumerate(samples))
+        select_prompt = (
+            f"Three independent attempts to answer the same request are below. "
+            f"Identify which is most consistent across factual claims and most defensible. "
+            f"If two largely agree on facts but differ in framing, prefer the clearer one. "
+            f"Output ONLY the chosen full answer text, with no preamble or sample number.\n\n"
+            f"REQUEST: {user_input}\n\n{labelled}"
+        )
+        select_overrides = dict(gen_overrides or {})
+        select_overrides["temperature"] = 0.2
+        chosen = self._get_chat_response(
+            select_prompt, working_context,
+            reasoning_mode="self_consistency", gen_overrides=select_overrides,
+            situation_brief=situation_brief,
+        )
+        print(f"[REASONING][SelfConsistency] selected ({len(chosen)} chars)")
+        return chosen
+
+    def _supports_mode_algorithm(self, mode: str) -> bool:
+        """Modes whose algorithm goes through the multi-stage helpers above."""
+        return mode in {"tree_of_thoughts", "constitutional_ai", "self_consistency"}
+
+    def _run_mode_algorithm(self, mode: str, user_input: str, working_context: str,
+                            gen_overrides: Dict[str, Any], situation_brief: str) -> Optional[str]:
+        """Dispatch to the per-mode algorithm. Returns None if mode is not algorithmic."""
+        if mode == "tree_of_thoughts":
+            return self._run_tree_of_thoughts(user_input, working_context, gen_overrides, situation_brief)
+        if mode == "constitutional_ai":
+            return self._run_constitutional_ai(user_input, working_context, gen_overrides, situation_brief)
+        if mode == "self_consistency":
+            return self._run_self_consistency(user_input, working_context, gen_overrides, situation_brief)
+        return None
+
     def _chat_generation_overrides(self, user_input: str, memory_context: str,
                                    reasoning_mode: Optional[str] = None) -> Dict[str, Any]:
         base = self._generation_settings()
diff --git a/eli/runtime/control_contracts.py b/eli/runtime/control_contracts.py
index b7cc4bc..21f31e0 100644
--- a/eli/runtime/control_contracts.py
+++ b/eli/runtime/control_contracts.py
@@ -198,6 +198,21 @@ def build_control_evidence(engine: Any, action: Any, args: Dict[str, Any] | None
         }
 
     if act == "USER_IDENTITY_SUMMARY":
+        try:
+            from eli.runtime.live_introspection import build_report
+            rep = build_report(act)
+            text = str((rep or {}).get("content") or (rep or {}).get("response") or "")
+            if text.strip():
+                return {
+                    "ok": True,
+                    "action": act,
+                    "content": text,
+                    "response": text,
+                    "report": (rep or {}).get("report") or {"source": "live_introspection"},
+                    "evidence_source": "live_introspection",
+                }
+        except Exception:
+            pass
         try:
             from eli.runtime.personal_memory_surface import personal_memory_surface
             text = str(personal_memory_surface(user_input))
diff --git a/eli/runtime/execution_intent_packets.py b/eli/runtime/execution_intent_packets.py
deleted file mode 100644
index 2b33800..0000000
--- a/eli/runtime/execution_intent_packets.py
+++ /dev/null
@@ -1 +0,0 @@
-from eli.execution.execution_intent_packets import *  # noqa: F401,F403
diff --git a/eli/runtime/execution_planner.py b/eli/runtime/execution_planner.py
deleted file mode 100644
index 59438b3..0000000
--- a/eli/runtime/execution_planner.py
+++ /dev/null
@@ -1 +0,0 @@
-from eli.execution.execution_planner import *  # noqa: F401,F403
diff --git a/eli/runtime/tool_execution_authority.py b/eli/runtime/tool_execution_authority.py
deleted file mode 100644
index 2666258..0000000
--- a/eli/runtime/tool_execution_authority.py
+++ /dev/null
@@ -1 +0,0 @@
-from eli.execution.tool_execution_authority import *  # noqa: F401,F403
diff --git a/packaging/debian/build-deb.sh b/packaging/debian/build-deb.sh
deleted file mode 100755
index 2279786..0000000
--- a/packaging/debian/build-deb.sh
+++ /dev/null
@@ -1,102 +0,0 @@
-#!/usr/bin/env bash
-# Build a Debian package for ELI MKXI.
-#
-# Usage:
-#   bash packaging/debian/build-deb.sh [VERSION]
-#
-# Output: dist/eli-mkxi_<VERSION>_amd64.deb
-set -euo pipefail
-
-PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
-PACKAGING="$PROJECT_ROOT/packaging/debian"
-VERSION="${1:-$(grep -E '^version' "$PROJECT_ROOT/pyproject.toml" | head -1 | awk -F'"' '{print $2}')}"
-ARCH="amd64"
-PKGNAME="eli-mkxi_${VERSION}_${ARCH}"
-STAGE="$PROJECT_ROOT/build/deb-stage/${PKGNAME}"
-DIST="$PROJECT_ROOT/dist"
-
-if ! command -v dpkg-deb >/dev/null; then
-    echo "[deb] ERROR: dpkg-deb not found. Install dpkg-dev (apt: dpkg-dev fakeroot)." >&2
-    exit 1
-fi
-
-echo "[deb] version=${VERSION} arch=${ARCH}"
-mkdir -p "$DIST"
-rm -rf "$STAGE"
-
-# ── 1. Layout ───────────────────────────────────────────────────────────
-mkdir -p "$STAGE/DEBIAN"
-mkdir -p "$STAGE/opt/eli-mkxi"
-mkdir -p "$STAGE/opt/eli-mkxi/dist"
-mkdir -p "$STAGE/usr/bin"
-mkdir -p "$STAGE/usr/share/applications"
-mkdir -p "$STAGE/usr/share/doc/eli-mkxi"
-mkdir -p "$STAGE/usr/share/icons/hicolor/256x256/apps"
-
-# ── 2. Build wheel first (sole source of code) ──────────────────────────
-echo "[deb] Building wheel…"
-( cd "$PROJECT_ROOT" && python3 -m build --wheel --outdir "$STAGE/opt/eli-mkxi/dist/" >/dev/null 2>&1 \
-  || python3 -m pip wheel --no-deps "$PROJECT_ROOT" -w "$STAGE/opt/eli-mkxi/dist/" )
-
-# Source tree (so editable fallback works without pulling from PyPI).
-# rsync excludes generated / runtime / cache directories that shouldn't ship
-# in a system package.
-RSYNC_EXCLUDES=(
-    --exclude '__pycache__'
-    --exclude '*.pyc'
-    --exclude '*.pyo'
-    --exclude '.pytest_cache'
-    --exclude 'artifacts'
-    --exclude '.venv'
-)
-rsync -a "${RSYNC_EXCLUDES[@]}" "$PROJECT_ROOT/eli/"     "$STAGE/opt/eli-mkxi/eli/"
-rsync -a "${RSYNC_EXCLUDES[@]}" "$PROJECT_ROOT/config/"  "$STAGE/opt/eli-mkxi/config/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/pyproject.toml" "$STAGE/opt/eli-mkxi/"
-cp    "$PROJECT_ROOT/requirements.txt" "$STAGE/opt/eli-mkxi/"
-cp    "$PROJECT_ROOT/requirements-full.txt" "$STAGE/opt/eli-mkxi/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/requirements-android.txt" "$STAGE/opt/eli-mkxi/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/requirements-windows.txt" "$STAGE/opt/eli-mkxi/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/requirements-macos.txt" "$STAGE/opt/eli-mkxi/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/README.md" "$STAGE/opt/eli-mkxi/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/.env.example" "$STAGE/opt/eli-mkxi/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/.env.full.example" "$STAGE/opt/eli-mkxi/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/eli.sh"  "$STAGE/opt/eli-mkxi/eli.sh"
-chmod +x "$STAGE/opt/eli-mkxi/eli.sh"
-
-# ── 3. Desktop / icon / docs ────────────────────────────────────────────
-cp "$PACKAGING/eli-mkxi.desktop"  "$STAGE/usr/share/applications/eli-mkxi.desktop"
-cp "$PACKAGING/copyright"         "$STAGE/usr/share/doc/eli-mkxi/copyright"
-cp "$PROJECT_ROOT/README.md"      "$STAGE/usr/share/doc/eli-mkxi/README.md" 2>/dev/null || true
-if [ -f "$PROJECT_ROOT/packaging/assets/eli-mkxi.png" ]; then
-    cp "$PROJECT_ROOT/packaging/assets/eli-mkxi.png" \
-       "$STAGE/usr/share/icons/hicolor/256x256/apps/eli-mkxi.png"
-fi
-
-# ── 4. Control file ─────────────────────────────────────────────────────
-INSTALLED_KB=$(du -sk "$STAGE/opt" "$STAGE/usr" | awk '{s+=$1} END {print s}')
-sed -e "s/@VERSION@/${VERSION}/" \
-    -e "s/@INSTALLED_SIZE@/${INSTALLED_KB}/" \
-    "$PACKAGING/control.in" > "$STAGE/DEBIAN/control"
-
-cp "$PACKAGING/postinst" "$STAGE/DEBIAN/postinst"
-cp "$PACKAGING/prerm"    "$STAGE/DEBIAN/prerm"
-cp "$PACKAGING/postrm"   "$STAGE/DEBIAN/postrm"
-chmod 0755 "$STAGE/DEBIAN/postinst" "$STAGE/DEBIAN/prerm" "$STAGE/DEBIAN/postrm"
-
-# ── 5. Build .deb (fakeroot if available) ───────────────────────────────
-OUTPUT="$DIST/${PKGNAME}.deb"
-if command -v fakeroot >/dev/null; then
-    fakeroot dpkg-deb --build --root-owner-group "$STAGE" "$OUTPUT"
-else
-    dpkg-deb --build --root-owner-group "$STAGE" "$OUTPUT"
-fi
-
-echo "[deb] Built: $OUTPUT  ($(du -h "$OUTPUT" | cut -f1))"
-
-# ── 6. Lint with lintian if available (non-fatal) ───────────────────────
-if command -v lintian >/dev/null; then
-    echo "[deb] lintian:"
-    lintian --no-tag-display-limit "$OUTPUT" || true
-fi
-
-echo "[deb] Done."
diff --git a/packaging/debian/control.in b/packaging/debian/control.in
deleted file mode 100644
index 0631294..0000000
--- a/packaging/debian/control.in
+++ /dev/null
@@ -1,19 +0,0 @@
-Package: eli-mkxi
-Version: @VERSION@
-Section: science
-Priority: optional
-Architecture: amd64
-Maintainer: Jay Bridgeman <jaybridgeman0095@gmail.com>
-Depends: python3 (>= 3.10), python3-venv, python3-pip, libxcb-cursor0, libxkbcommon0, libgl1, libegl1, libfontconfig1, libdbus-1-3, libnss3, libxrandr2, libxcomposite1, libxdamage1, libxfixes3, libasound2 | libasound2t64, ffmpeg
-Recommends: nvidia-driver, cuda-toolkit-12-1, texlive-luatex, pandoc
-Installed-Size: @INSTALLED_SIZE@
-Homepage: https://github.com/ShadowESC95/ELI_MKXI
-Description: ELI MKXI — local AI assistant
- ELI MKXI is a fully offline AI assistant with multi-agent reasoning,
- local GGUF inference (CPU + CUDA), persistent SQLite memory,
- vector search (FAISS), and a PySide6 GUI. Includes a thesis-grade
- reports workspace, image engine, and 3-hour news synthesis cadence.
- .
- First launch creates ~/.local/share/eli/ for user data; the package
- ships its Python virtualenv under /opt/eli-mkxi and links a launcher
- to /usr/bin/eli-mkxi.
diff --git a/packaging/debian/copyright b/packaging/debian/copyright
deleted file mode 100644
index 1f97104..0000000
--- a/packaging/debian/copyright
+++ /dev/null
@@ -1,8 +0,0 @@
-Format: https://www.debian.org/doc/packaging-manuals/copyright-format/1.0/
-Upstream-Name: ELI MKXI
-Source: https://github.com/ShadowESC95/ELI_MKXI
-
-Files: *
-Copyright: 2026 Jay Bridgeman
-License: Proprietary
- All rights reserved. Redistribution requires written permission.
diff --git a/packaging/debian/eli-mkxi.desktop b/packaging/debian/eli-mkxi.desktop
deleted file mode 100644
index f259093..0000000
--- a/packaging/debian/eli-mkxi.desktop
+++ /dev/null
@@ -1,12 +0,0 @@
-[Desktop Entry]
-Name=ELI MKXI
-GenericName=Local AI Assistant
-Comment=100% local multi-agent assistant with GPU-accelerated GGUF inference
-Exec=eli-mkxi %U
-Icon=eli-mkxi
-Terminal=false
-Type=Application
-Categories=Utility;Science;ArtificialIntelligence;Development;Education;
-StartupNotify=true
-StartupWMClass=eli-mkxi
-Keywords=AI;assistant;LLM;local;GPU;research;
diff --git a/packaging/debian/postinst b/packaging/debian/postinst
deleted file mode 100755
index 34c82a4..0000000
--- a/packaging/debian/postinst
+++ /dev/null
@@ -1,48 +0,0 @@
-#!/usr/bin/env bash
-# eli-mkxi postinst — finalise the install
-set -e
-
-INSTALL_ROOT="/opt/eli-mkxi"
-LAUNCHER="/usr/bin/eli-mkxi"
-DESKTOP_DIR="/usr/share/applications"
-
-echo "[eli-mkxi] Configuring at $INSTALL_ROOT"
-
-# Build venv on the target machine so torch/llama-cpp-python pick the
-# correct CPU/GPU wheels for the host.
-PYTHON="$(command -v python3)"
-if [ -z "$PYTHON" ]; then
-    echo "[eli-mkxi] python3 not found; aborting" >&2
-    exit 1
-fi
-
-VENV="$INSTALL_ROOT/.venv"
-if [ ! -d "$VENV" ]; then
-    echo "[eli-mkxi] Creating virtualenv at $VENV"
-    "$PYTHON" -m venv "$VENV"
-fi
-
-PIP="$VENV/bin/pip"
-"$PIP" install --upgrade pip setuptools wheel >/dev/null
-
-WHEEL=$(ls "$INSTALL_ROOT"/dist/eli_mkxi-*.whl 2>/dev/null | head -1 || true)
-if [ -n "$WHEEL" ]; then
-    echo "[eli-mkxi] Installing wheel: $(basename "$WHEEL")"
-    "$PIP" install --upgrade "$WHEEL"[full] >/dev/null
-fi
-
-# Default to CPU torch + llama-cpp-python; users can switch to CUDA via
-#   /opt/eli-mkxi/.venv/bin/pip install torch --index-url https://download.pytorch.org/whl/cu121
-"$PIP" install torch --index-url https://download.pytorch.org/whl/cpu --upgrade >/dev/null || true
-"$PIP" install llama-cpp-python --prefer-binary --upgrade >/dev/null || true
-
-# Make launcher executable & symlink
-chmod +x "$INSTALL_ROOT/eli.sh"
-ln -sf "$INSTALL_ROOT/eli.sh" "$LAUNCHER"
-
-# Refresh desktop db & icon cache (best-effort)
-update-desktop-database "$DESKTOP_DIR" >/dev/null 2>&1 || true
-gtk-update-icon-cache -f -t /usr/share/icons/hicolor >/dev/null 2>&1 || true
-
-echo "[eli-mkxi] Install complete. Launch with: eli-mkxi"
-exit 0
diff --git a/packaging/debian/postrm b/packaging/debian/postrm
deleted file mode 100755
index d6d880f..0000000
--- a/packaging/debian/postrm
+++ /dev/null
@@ -1,17 +0,0 @@
-#!/usr/bin/env bash
-set -e
-
-case "$1" in
-    purge)
-        rm -rf /opt/eli-mkxi/.venv
-        rm -f /usr/share/applications/eli-mkxi.desktop
-        rm -f /usr/share/icons/hicolor/256x256/apps/eli-mkxi.png
-        update-desktop-database /usr/share/applications >/dev/null 2>&1 || true
-        gtk-update-icon-cache -f -t /usr/share/icons/hicolor >/dev/null 2>&1 || true
-        ;;
-    remove|upgrade|disappear|abort-install|abort-upgrade)
-        ;;
-    *)
-        ;;
-esac
-exit 0
diff --git a/packaging/debian/prerm b/packaging/debian/prerm
deleted file mode 100755
index 55fbda9..0000000
--- a/packaging/debian/prerm
+++ /dev/null
@@ -1,15 +0,0 @@
-#!/usr/bin/env bash
-set -e
-
-case "$1" in
-    remove|upgrade|deconfigure)
-        rm -f /usr/bin/eli-mkxi
-        ;;
-    failed-upgrade)
-        ;;
-    *)
-        echo "prerm called with unknown argument: $1" >&2
-        exit 1
-        ;;
-esac
-exit 0
diff --git a/packaging/linux/appimage/AppRun b/packaging/linux/appimage/AppRun
deleted file mode 100755
index 2587d9b..0000000
--- a/packaging/linux/appimage/AppRun
+++ /dev/null
@@ -1,13 +0,0 @@
-#!/usr/bin/env bash
-# ELI MKXI AppImage entry point.
-HERE="$(dirname "$(readlink -f "$0")")"
-export PYTHONHOME=
-export PATH="$HERE/usr/bin:$PATH"
-export LD_LIBRARY_PATH="$HERE/usr/lib:${LD_LIBRARY_PATH:-}"
-export ELI_PROJECT_ROOT="$HERE/usr/lib/eli-mkxi"
-
-# Use a per-user data dir so AppImages are stateless on the host.
-export ELI_DATA_DIR="${ELI_DATA_DIR:-$HOME/.local/share/eli}"
-export ELI_CONFIG_DIR="${ELI_CONFIG_DIR:-$HOME/.config/eli}"
-
-exec "$HERE/usr/lib/eli-mkxi/.venv/bin/python" -m eli "$@"
diff --git a/packaging/linux/appimage/eli-mkxi.desktop b/packaging/linux/appimage/eli-mkxi.desktop
deleted file mode 100644
index de879a3..0000000
--- a/packaging/linux/appimage/eli-mkxi.desktop
+++ /dev/null
@@ -1,10 +0,0 @@
-[Desktop Entry]
-Name=ELI MKXI
-Comment=Local AI Assistant
-Exec=AppRun
-Icon=eli-mkxi
-Type=Application
-Categories=Utility;Science;ArtificialIntelligence;
-Terminal=false
-StartupNotify=true
-StartupWMClass=eli-mkxi
diff --git a/packaging/linux/build-appimage.sh b/packaging/linux/build-appimage.sh
deleted file mode 100755
index 65d398f..0000000
--- a/packaging/linux/build-appimage.sh
+++ /dev/null
@@ -1,73 +0,0 @@
-#!/usr/bin/env bash
-# Build an ELI MKXI AppImage.
-#
-# Requires `appimagetool` on PATH (https://github.com/AppImage/AppImageKit/releases).
-# Falls back to a portable tar.gz if appimagetool is not available.
-#
-# Usage:
-#   bash packaging/linux/build-appimage.sh [VERSION]
-set -euo pipefail
-
-PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
-VERSION="${1:-$(grep -E '^version' "$PROJECT_ROOT/pyproject.toml" | head -1 | awk -F'"' '{print $2}')}"
-ARCH="x86_64"
-
-DIST="$PROJECT_ROOT/dist"
-APPDIR="$PROJECT_ROOT/build/AppDir"
-RES="$PROJECT_ROOT/packaging/linux/appimage"
-INSTALL_PREFIX="$APPDIR/usr/lib/eli-mkxi"
-
-mkdir -p "$DIST"
-rm -rf "$APPDIR"
-mkdir -p "$APPDIR/usr/bin" "$INSTALL_PREFIX" "$APPDIR/usr/share/applications" \
-         "$APPDIR/usr/share/icons/hicolor/256x256/apps"
-
-echo "[AppImage] Staging source tree…"
-cp -r "$PROJECT_ROOT/eli"         "$INSTALL_PREFIX/"
-cp -r "$PROJECT_ROOT/config"      "$INSTALL_PREFIX/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/pyproject.toml" "$INSTALL_PREFIX/"
-cp    "$PROJECT_ROOT/README.md" "$INSTALL_PREFIX/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/.env.example" "$INSTALL_PREFIX/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/.env.full.example" "$INSTALL_PREFIX/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/requirements.txt" "$INSTALL_PREFIX/"
-cp    "$PROJECT_ROOT/requirements-full.txt" "$INSTALL_PREFIX/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/requirements-android.txt" "$INSTALL_PREFIX/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/requirements-windows.txt" "$INSTALL_PREFIX/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/requirements-macos.txt" "$INSTALL_PREFIX/" 2>/dev/null || true
-
-echo "[AppImage] Building target venv (CPU defaults; GPU users override after install)…"
-python3 -m venv "$INSTALL_PREFIX/.venv"
-"$INSTALL_PREFIX/.venv/bin/pip" install --quiet --upgrade pip setuptools wheel
-"$INSTALL_PREFIX/.venv/bin/pip" install --quiet "$INSTALL_PREFIX"[full] || \
-    "$INSTALL_PREFIX/.venv/bin/pip" install --quiet -e "$INSTALL_PREFIX"[full]
-"$INSTALL_PREFIX/.venv/bin/pip" install --quiet \
-    torch --index-url https://download.pytorch.org/whl/cpu || true
-"$INSTALL_PREFIX/.venv/bin/pip" install --quiet \
-    llama-cpp-python --prefer-binary || true
-
-# AppRun + desktop + icon
-cp  "$RES/AppRun"             "$APPDIR/AppRun"
-cp  "$RES/eli-mkxi.desktop"   "$APPDIR/eli-mkxi.desktop"
-cp  "$RES/eli-mkxi.desktop"   "$APPDIR/usr/share/applications/eli-mkxi.desktop"
-chmod +x "$APPDIR/AppRun"
-
-if [ -f "$PROJECT_ROOT/packaging/assets/eli-mkxi.png" ]; then
-    cp "$PROJECT_ROOT/packaging/assets/eli-mkxi.png" "$APPDIR/eli-mkxi.png"
-    cp "$PROJECT_ROOT/packaging/assets/eli-mkxi.png" \
-       "$APPDIR/usr/share/icons/hicolor/256x256/apps/eli-mkxi.png"
-fi
-
-if command -v appimagetool >/dev/null; then
-    OUT="$DIST/ELI_MKXI-${VERSION}-${ARCH}.AppImage"
-    echo "[AppImage] Running appimagetool → $OUT"
-    ARCH=$ARCH appimagetool "$APPDIR" "$OUT"
-    chmod +x "$OUT"
-    echo "[AppImage] Built: $OUT"
-else
-    OUT="$DIST/eli-mkxi-${VERSION}-${ARCH}-portable.tar.gz"
-    echo "[AppImage] appimagetool not found; emitting portable tarball: $OUT"
-    tar -czf "$OUT" -C "$(dirname "$APPDIR")" "$(basename "$APPDIR")"
-    echo "[AppImage] Wrote portable tarball: $OUT"
-fi
-
-echo "[AppImage] Done."
diff --git a/packaging/macos/Info.plist b/packaging/macos/Info.plist
deleted file mode 100644
index e13e747..0000000
--- a/packaging/macos/Info.plist
+++ /dev/null
@@ -1,24 +0,0 @@
-<?xml version="1.0" encoding="UTF-8"?>
-<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
-<plist version="1.0">
-<dict>
-    <key>CFBundleName</key>             <string>ELI MKXI</string>
-    <key>CFBundleDisplayName</key>      <string>ELI MKXI</string>
-    <key>CFBundleIdentifier</key>       <string>com.shadowesc95.eli-mkxi</string>
-    <key>CFBundleVersion</key>          <string>@VERSION@</string>
-    <key>CFBundleShortVersionString</key><string>@VERSION@</string>
-    <key>CFBundleExecutable</key>       <string>eli-mkxi</string>
-    <key>CFBundleIconFile</key>         <string>eli-mkxi.icns</string>
-    <key>CFBundlePackageType</key>      <string>APPL</string>
-    <key>CFBundleSignature</key>        <string>????</string>
-    <key>NSHighResolutionCapable</key>  <true/>
-    <key>LSMinimumSystemVersion</key>   <string>11.0</string>
-    <key>LSApplicationCategoryType</key><string>public.app-category.productivity</string>
-    <key>NSMicrophoneUsageDescription</key>
-        <string>ELI MKXI uses the microphone for local speech-to-text. Audio never leaves the device.</string>
-    <key>NSSpeechRecognitionUsageDescription</key>
-        <string>ELI MKXI processes speech locally; no audio is uploaded.</string>
-    <key>NSCameraUsageDescription</key>
-        <string>ELI MKXI uses the camera only when explicitly invoked. No frames are uploaded.</string>
-</dict>
-</plist>
diff --git a/packaging/macos/build-macos-app.sh b/packaging/macos/build-macos-app.sh
deleted file mode 100755
index 372e4ad..0000000
--- a/packaging/macos/build-macos-app.sh
+++ /dev/null
@@ -1,93 +0,0 @@
-#!/usr/bin/env bash
-# Build a macOS .app bundle and a distributable .dmg for ELI MKXI.
-#
-# This is intended to run on macOS (uses hdiutil, sips/iconutil). On Linux it
-# emits the .app layout as a tarball so users can finalise on their Mac.
-#
-# Usage:
-#   bash packaging/macos/build-macos-app.sh [VERSION]
-set -euo pipefail
-
-PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
-VERSION="${1:-$(grep -E '^version' "$PROJECT_ROOT/pyproject.toml" | head -1 | awk -F'"' '{print $2}')}"
-DIST="$PROJECT_ROOT/dist"
-BUILD="$PROJECT_ROOT/build/macos"
-APP="$BUILD/ELI MKXI.app"
-RES="$PROJECT_ROOT/packaging/macos"
-
-mkdir -p "$DIST"
-rm -rf "$BUILD"
-mkdir -p "$APP/Contents/MacOS" "$APP/Contents/Resources"
-
-echo "[macos] Building app bundle: $APP"
-
-# ── Resources: source + venv ────────────────────────────────────────────
-cp -R "$PROJECT_ROOT/eli"         "$APP/Contents/Resources/"
-cp -R "$PROJECT_ROOT/config"      "$APP/Contents/Resources/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/pyproject.toml" "$APP/Contents/Resources/"
-cp    "$PROJECT_ROOT/README.md" "$APP/Contents/Resources/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/.env.example" "$APP/Contents/Resources/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/.env.full.example" "$APP/Contents/Resources/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/requirements-full.txt" "$APP/Contents/Resources/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/requirements-android.txt" "$APP/Contents/Resources/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/requirements-windows.txt" "$APP/Contents/Resources/" 2>/dev/null || true
-cp    "$PROJECT_ROOT/requirements-macos.txt" "$APP/Contents/Resources/requirements.txt"
-cp    "$PROJECT_ROOT/requirements-macos.txt" "$APP/Contents/Resources/requirements-macos.txt"
-
-if command -v python3 >/dev/null; then
-    python3 -m venv "$APP/Contents/Resources/.venv"
-    "$APP/Contents/Resources/.venv/bin/pip" install --quiet --upgrade pip setuptools wheel
-    "$APP/Contents/Resources/.venv/bin/pip" install --quiet \
-        "$APP/Contents/Resources"[full] 2>/dev/null || \
-    "$APP/Contents/Resources/.venv/bin/pip" install --quiet \
-        -e "$APP/Contents/Resources"[full]
-    if [ "$(uname -s)" = "Darwin" ]; then
-        # MPS-capable torch + Metal-accelerated llama
-        "$APP/Contents/Resources/.venv/bin/pip" install --quiet torch torchvision torchaudio || true
-        CMAKE_ARGS="-DLLAMA_METAL=on" "$APP/Contents/Resources/.venv/bin/pip" \
-            install --quiet llama-cpp-python --prefer-binary || true
-    fi
-fi
-
-# ── Launcher binary ─────────────────────────────────────────────────────
-cp "$RES/launcher.sh" "$APP/Contents/MacOS/eli-mkxi"
-chmod +x "$APP/Contents/MacOS/eli-mkxi"
-
-# ── Info.plist ──────────────────────────────────────────────────────────
-sed -e "s/@VERSION@/${VERSION}/g" "$RES/Info.plist" > "$APP/Contents/Info.plist"
-
-# ── Icon (.icns) ────────────────────────────────────────────────────────
-if [ -f "$PROJECT_ROOT/packaging/assets/eli-mkxi.icns" ]; then
-    cp "$PROJECT_ROOT/packaging/assets/eli-mkxi.icns" "$APP/Contents/Resources/eli-mkxi.icns"
-elif [ -f "$PROJECT_ROOT/packaging/assets/eli-mkxi.png" ] && command -v iconutil >/dev/null; then
-    ICONSET="$BUILD/eli-mkxi.iconset"
-    mkdir -p "$ICONSET"
-    SRC="$PROJECT_ROOT/packaging/assets/eli-mkxi.png"
-    for s in 16 32 64 128 256 512 1024; do
-        sips -z $s $s "$SRC" --out "$ICONSET/icon_${s}x${s}.png" >/dev/null 2>&1 || true
-        sips -z $((s*2)) $((s*2)) "$SRC" --out "$ICONSET/icon_${s}x${s}@2x.png" >/dev/null 2>&1 || true
-    done
-    iconutil -c icns "$ICONSET" -o "$APP/Contents/Resources/eli-mkxi.icns"
-    rm -rf "$ICONSET"
-fi
-
-# ── Pack into .dmg (macOS only) ─────────────────────────────────────────
-if [ "$(uname -s)" = "Darwin" ] && command -v hdiutil >/dev/null; then
-    DMG="$DIST/ELI_MKXI-${VERSION}.dmg"
-    rm -f "$DMG"
-    STAGE_DMG="$BUILD/dmg-stage"
-    mkdir -p "$STAGE_DMG"
-    cp -R "$APP" "$STAGE_DMG/"
-    ln -sfn /Applications "$STAGE_DMG/Applications"
-    hdiutil create -volname "ELI MKXI ${VERSION}" \
-                   -srcfolder "$STAGE_DMG" \
-                   -ov -format UDZO "$DMG"
-    echo "[macos] Built DMG: $DMG"
-else
-    OUT="$DIST/ELI_MKXI-${VERSION}-macos-app.tar.gz"
-    tar -czf "$OUT" -C "$BUILD" "ELI MKXI.app"
-    echo "[macos] Built app tarball: $OUT"
-    echo "[macos] (Run on macOS to produce a .dmg via hdiutil.)"
-fi
-
-echo "[macos] Done."
diff --git a/packaging/macos/launcher.sh b/packaging/macos/launcher.sh
deleted file mode 100755
index d318de0..0000000
--- a/packaging/macos/launcher.sh
+++ /dev/null
@@ -1,19 +0,0 @@
-#!/usr/bin/env bash
-# ELI MKXI macOS launcher (used as the app bundle's MacOS/eli-mkxi binary).
-set -e
-
-HERE="$(cd "$(dirname "$0")" && pwd)"
-RESOURCES="$HERE/../Resources"
-VENV="$RESOURCES/.venv"
-PYTHON="$VENV/bin/python"
-
-if [ ! -x "$PYTHON" ]; then
-    osascript -e 'display dialog "ELI MKXI cannot find its Python venv inside the app bundle. Re-run install.sh from the bundle root." buttons {"OK"} default button "OK"'
-    exit 1
-fi
-
-export ELI_PROJECT_ROOT="$RESOURCES"
-export ELI_DATA_DIR="${ELI_DATA_DIR:-$HOME/Library/Application Support/ELI MKXI}"
-export ELI_CONFIG_DIR="${ELI_CONFIG_DIR:-$HOME/Library/Preferences/ELI MKXI}"
-
-exec "$PYTHON" -m eli "$@"
diff --git a/packaging/windows/build-windows.ps1 b/packaging/windows/build-windows.ps1
deleted file mode 100644
index 371a8a9..0000000
--- a/packaging/windows/build-windows.ps1
+++ /dev/null
@@ -1,189 +0,0 @@
-# Build a Windows installer for ELI MKXI.
-# Run on Windows in PowerShell:
-#   powershell -ExecutionPolicy Bypass -File packaging\windows\build-windows.ps1
-#
-# Tries Inno Setup (ISCC.exe) first, then NSIS (makensis.exe).
-# Falls back to a portable zip if neither is installed.
-param(
-    [string]$Version = "",
-    [switch]$SkipWheelhouse,
-    [string[]]$WheelhousePythonVersions = @("311", "312")
-)
-
-$ErrorActionPreference = "Stop"
-$ProjectRoot = Resolve-Path "$PSScriptRoot\..\.."
-Set-Location $ProjectRoot
-
-if (-not $Version) {
-    $line = Select-String -Path "pyproject.toml" -Pattern '^version' | Select-Object -First 1
-    $Version = ($line.Line -split '"')[1]
-}
-
-Write-Host "[windows] version=$Version" -ForegroundColor Cyan
-
-$Dist = Join-Path $ProjectRoot "dist"
-New-Item -ItemType Directory -Force -Path $Dist | Out-Null
-$Wheelhouse = Join-Path $Dist "wheelhouse"
-
-function Test-BuildFrontend {
-    $oldLocation = Get-Location
-    try {
-        Set-Location $env:TEMP
-        & python -c "import build.__main__" *> $null
-        return ($LASTEXITCODE -eq 0)
-    } finally {
-        Set-Location $oldLocation
-    }
-}
-
-function Build-ProjectWheel {
-    if (Test-BuildFrontend) {
-        $oldLocation = Get-Location
-        try {
-            Set-Location $env:TEMP
-            & python -m build $ProjectRoot --wheel --outdir $Dist
-        } finally {
-            Set-Location $oldLocation
-        }
-    } else {
-        Write-Host "[windows] python-build frontend not installed; building wheel via pip wheel." -ForegroundColor Yellow
-        & python -m pip wheel --no-deps $ProjectRoot -w $Dist
-    }
-    if ($LASTEXITCODE -ne 0) {
-        throw "Wheel build failed."
-    }
-}
-
-# Make sure the wheel is present so the installer can ship it.
-if (-not (Test-Path "$Dist\eli_mkxi-*.whl")) {
-    Write-Host "[windows] Building wheel..." -ForegroundColor Yellow
-    Build-ProjectWheel
-}
-
-function Build-Wheelhouse {
-    if ($SkipWheelhouse) {
-        Write-Host "[windows] Skipping wheelhouse build." -ForegroundColor Yellow
-        return
-    }
-
-    New-Item -ItemType Directory -Force -Path $Wheelhouse | Out-Null
-    $requirements = Get-Content "$ProjectRoot\requirements-windows.txt" |
-        ForEach-Object { ($_ -replace '\s+#.*$', '').Trim() } |
-        Where-Object { $_ -and -not $_.StartsWith("#") }
-
-    foreach ($pyver in $WheelhousePythonVersions) {
-        $abi = "cp$pyver"
-        Write-Host "[windows] Downloading Windows wheelhouse for CPython $pyver..." -ForegroundColor Yellow
-        foreach ($req in $requirements) {
-            if ($req -like "PyAutoGUI*" -or $req -like "pyautogui*") {
-                continue
-            }
-            $extraDownloadArgs = @()
-            if ($req -like "accelerate*") {
-                $extraDownloadArgs += "--no-deps"
-            }
-            & python -m pip download `
-                --dest $Wheelhouse `
-                --platform win_amd64 `
-                --python-version $pyver `
-                --implementation cp `
-                --abi $abi `
-                --only-binary=:all: `
-                --prefer-binary `
-                @extraDownloadArgs `
-                $req
-            if ($LASTEXITCODE -ne 0) {
-                Write-Warning "Missing binary wheel for CPython ${pyver}: $req"
-            }
-        }
-
-        Write-Host "[windows] Downloading CPU PyTorch wheel for CPython $pyver..." -ForegroundColor Yellow
-        & python -m pip download `
-            --dest $Wheelhouse `
-            --platform win_amd64 `
-            --python-version $pyver `
-            --implementation cp `
-            --abi $abi `
-            --only-binary=:all: `
-            --prefer-binary `
-            torch `
-            --index-url https://download.pytorch.org/whl/cpu
-        if ($LASTEXITCODE -ne 0) {
-            Write-Warning "Missing CPU PyTorch wheel for CPython $pyver."
-        }
-    }
-
-    Write-Host "[windows] Building pure-Python automation wheels..." -ForegroundColor Yellow
-    & python -m pip wheel `
-        --wheel-dir $Wheelhouse `
-        "PyAutoGUI>=0.9.54"
-    if ($LASTEXITCODE -ne 0) {
-        Write-Warning "Missing pure-Python automation wheels for PyAutoGUI."
-    }
-}
-
-Build-Wheelhouse
-
-function Try-InnoSetup {
-    $iscc = Get-Command "ISCC.exe" -ErrorAction SilentlyContinue
-    if ($iscc) {
-        Write-Host "[windows] Found Inno Setup at $($iscc.Path)" -ForegroundColor Green
-        & $iscc.Path "/DMyAppVersion=$Version" "$ProjectRoot\packaging\windows\eli-mkxi.iss"
-        if ($LASTEXITCODE -eq 0) {
-            return $true
-        }
-        Write-Warning "Inno Setup failed; trying the next Windows package format."
-        return $false
-    }
-    return $false
-}
-
-function Try-NSIS {
-    $makensis = Get-Command "makensis.exe" -ErrorAction SilentlyContinue
-    if ($makensis) {
-        Write-Host "[windows] Found NSIS at $($makensis.Path)" -ForegroundColor Green
-        & $makensis.Path "/DVERSION=$Version" "$ProjectRoot\packaging\windows\eli-mkxi.nsi"
-        if ($LASTEXITCODE -eq 0) {
-            return $true
-        }
-        Write-Warning "NSIS failed; falling back to portable zip."
-        return $false
-    }
-    return $false
-}
-
-if (Try-InnoSetup) { exit 0 }
-if (Try-NSIS)      { exit 0 }
-
-# Portable zip fallback
-Write-Host "[windows] Neither Inno Setup nor NSIS found; producing portable zip." -ForegroundColor Yellow
-$zip = Join-Path $Dist "eli-mkxi-$Version-windows-portable.zip"
-$staging = Join-Path $env:TEMP "eli-mkxi-portable-$Version"
-if (Test-Path $staging) { Remove-Item -Recurse -Force $staging }
-New-Item -ItemType Directory -Force -Path $staging | Out-Null
-
-Copy-Item -Recurse -Path "$ProjectRoot\eli"     -Destination $staging
-if (Test-Path "$ProjectRoot\config") {
-    Copy-Item -Recurse -Path "$ProjectRoot\config"  -Destination $staging
-}
-Copy-Item "$ProjectRoot\pyproject.toml" -Destination $staging
-if (Test-Path "$ProjectRoot\README.md") { Copy-Item "$ProjectRoot\README.md" -Destination $staging }
-if (Test-Path "$ProjectRoot\.env.example") { Copy-Item "$ProjectRoot\.env.example" -Destination $staging }
-if (Test-Path "$ProjectRoot\.env.full.example") { Copy-Item "$ProjectRoot\.env.full.example" -Destination $staging }
-Copy-Item "$ProjectRoot\eli.bat"        -Destination $staging
-Copy-Item "$ProjectRoot\install.bat"    -Destination $staging
-Copy-Item "$ProjectRoot\install.ps1"    -Destination $staging
-Copy-Item "$ProjectRoot\requirements-full.txt" -Destination "$staging\requirements-full.txt"
-Copy-Item "$ProjectRoot\requirements-android.txt" -Destination "$staging\requirements-android.txt"
-Copy-Item "$ProjectRoot\requirements-macos.txt" -Destination "$staging\requirements-macos.txt"
-Copy-Item "$ProjectRoot\requirements-windows.txt" -Destination "$staging\requirements-windows.txt"
-Copy-Item "$ProjectRoot\requirements-windows.txt" -Destination "$staging\requirements.txt"
-New-Item -ItemType Directory -Force -Path "$staging\dist" | Out-Null
-Get-ChildItem "$Dist\eli_mkxi-*.whl" | Copy-Item -Destination "$staging\dist"
-if (Test-Path "$Wheelhouse\*.whl") {
-    Copy-Item -Recurse $Wheelhouse -Destination "$staging\wheelhouse"
-}
-
-if (Test-Path $zip) { Remove-Item $zip }
-Compress-Archive -Path "$staging\*" -DestinationPath $zip
-Write-Host "[windows] Portable zip: $zip" -ForegroundColor Green
diff --git a/packaging/windows/eli-mkxi.iss b/packaging/windows/eli-mkxi.iss
deleted file mode 100644
index 85e58bd..0000000
--- a/packaging/windows/eli-mkxi.iss
+++ /dev/null
@@ -1,92 +0,0 @@
-; Inno Setup script for ELI MKXI Windows installer
-; Build with: ISCC.exe packaging\windows\eli-mkxi.iss
-; Produces:   dist\ELI_MKXI-<VERSION>-Setup.exe
-
-#define MyAppName       "ELI MKXI"
-#ifndef MyAppVersion
-#define MyAppVersion    "1.0.0"
-#endif
-#define MyAppPublisher  "Jay Bridgeman"
-#define MyAppURL        "https://github.com/ShadowESC95/ELI_MKXI"
-#define MyAppExeName    "eli.bat"
-
-[Setup]
-AppId={{B68F9D9A-66D8-4F23-9B43-8A6F5E1D7D80}
-AppName={#MyAppName}
-AppVersion={#MyAppVersion}
-AppPublisher={#MyAppPublisher}
-AppPublisherURL={#MyAppURL}
-AppSupportURL={#MyAppURL}/issues
-AppUpdatesURL={#MyAppURL}/releases
-DefaultDirName={autopf}\ELI MKXI
-DefaultGroupName=ELI MKXI
-AllowNoIcons=yes
-LicenseFile=
-OutputDir=..\..\dist
-OutputBaseFilename=ELI_MKXI-{#MyAppVersion}-Setup
-#ifexist "..\assets\eli-mkxi.ico"
-SetupIconFile=..\assets\eli-mkxi.ico
-#endif
-Compression=lzma2/ultra
-SolidCompression=yes
-WizardStyle=modern
-ArchitecturesAllowed=x64
-ArchitecturesInstallIn64BitMode=x64
-PrivilegesRequired=lowest
-PrivilegesRequiredOverridesAllowed=dialog
-UninstallDisplayIcon={app}\packaging\assets\eli-mkxi.ico
-DisableProgramGroupPage=auto
-
-[Languages]
-Name: "english"; MessagesFile: "compiler:Default.isl"
-
-[Tasks]
-Name: "desktopicon";  Description: "{cm:CreateDesktopIcon}";    GroupDescription: "{cm:AdditionalIcons}"; Flags: unchecked
-Name: "quicklaunchicon"; Description: "{cm:CreateQuickLaunchIcon}"; GroupDescription: "{cm:AdditionalIcons}"; Flags: unchecked
-
-[Files]
-; Source tree
-Source: "..\..\eli\*";              DestDir: "{app}\eli";          Flags: recursesubdirs createallsubdirs ignoreversion
-Source: "..\..\config\*";           DestDir: "{app}\config";       Flags: recursesubdirs createallsubdirs ignoreversion skipifsourcedoesntexist
-Source: "..\..\pyproject.toml";     DestDir: "{app}";              Flags: ignoreversion
-Source: "..\..\requirements-windows.txt"; DestDir: "{app}";              Flags: ignoreversion
-Source: "..\..\requirements-windows.txt"; DestDir: "{app}"; DestName: "requirements.txt"; Flags: ignoreversion
-Source: "..\..\eli.bat";            DestDir: "{app}";              Flags: ignoreversion
-Source: "..\..\install.bat";        DestDir: "{app}";              Flags: ignoreversion
-Source: "..\..\install.ps1";        DestDir: "{app}";              Flags: ignoreversion
-Source: "..\..\dist\eli_mkxi-*.whl"; DestDir: "{app}\dist";        Flags: ignoreversion skipifsourcedoesntexist
-Source: "..\..\dist\wheelhouse\*";   DestDir: "{app}\wheelhouse";   Flags: recursesubdirs createallsubdirs ignoreversion skipifsourcedoesntexist
-Source: "..\assets\*";              DestDir: "{app}\packaging\assets"; Flags: ignoreversion skipifsourcedoesntexist
-
-[Icons]
-Name: "{group}\{#MyAppName}";       Filename: "{app}\{#MyAppExeName}";  IconFilename: "{app}\packaging\assets\eli-mkxi.ico"
-Name: "{group}\{cm:UninstallProgram,{#MyAppName}}"; Filename: "{uninstallexe}"
-Name: "{autodesktop}\{#MyAppName}"; Filename: "{app}\{#MyAppExeName}";  IconFilename: "{app}\packaging\assets\eli-mkxi.ico"; Tasks: desktopicon
-
-[Run]
-; First-run setup: build venv and install dependencies.
-Filename: "{cmd}"; Parameters: "/C ""{app}\install.bat"" /unattended"; WorkingDir: "{app}"; \
-    StatusMsg: "Setting up Python virtual environment & dependencies (first run only)…"; \
-    Flags: shellexec waituntilterminated runhidden
-Filename: "{app}\{#MyAppExeName}"; Description: "Launch {#MyAppName}"; Flags: postinstall skipifsilent shellexec
-
-[UninstallDelete]
-Type: filesandordirs; Name: "{app}\.venv"
-Type: filesandordirs; Name: "{app}\artifacts\db"
-
-[Code]
-function InitializeSetup(): Boolean;
-var
-    ResultCode: Integer;
-begin
-    Result := True;
-    if Exec('python', '-c "import sys; raise SystemExit(0 if sys.version_info >= (3, 11) else 1)"', '', SW_HIDE, ewWaitUntilTerminated, ResultCode) then begin
-        if ResultCode <> 0 then begin
-            MsgBox('Python 3.11+ is required. Install it from python.org and re-run this installer.', mbError, MB_OK);
-            Result := False;
-        end;
-    end else begin
-        MsgBox('Python was not found on PATH. Install Python 3.11+ from python.org and re-run.', mbError, MB_OK);
-        Result := False;
-    end;
-end;
diff --git a/packaging/windows/eli-mkxi.nsi b/packaging/windows/eli-mkxi.nsi
deleted file mode 100644
index 26fa106..0000000
--- a/packaging/windows/eli-mkxi.nsi
+++ /dev/null
@@ -1,90 +0,0 @@
-; NSIS script for ELI MKXI (alternative to Inno Setup).
-; Build with:  makensis packaging\windows\eli-mkxi.nsi
-; Output:      dist\ELI_MKXI-<VERSION>-Setup.exe
-
-!define APPNAME      "ELI MKXI"
-!define COMPANY      "Jay Bridgeman"
-!ifndef VERSION
-!define VERSION      "1.0.0"
-!endif
-!define APP_EXE      "eli.bat"
-!define INSTALL_KEY  "Software\Microsoft\Windows\CurrentVersion\Uninstall\ELI_MKXI"
-
-OutFile "..\..\dist\ELI_MKXI-${VERSION}-Setup.exe"
-InstallDir "$PROGRAMFILES64\ELI MKXI"
-InstallDirRegKey HKLM "Software\${COMPANY}\${APPNAME}" "InstallLocation"
-RequestExecutionLevel admin
-SetCompressor /SOLID lzma
-ShowInstDetails show
-ShowUnInstDetails show
-Name "${APPNAME} ${VERSION}"
-
-Page directory
-Page instfiles
-UninstPage uninstConfirm
-UninstPage instfiles
-
-Section "Install"
-    SetOutPath "$INSTDIR"
-
-    ; Source tree
-    File /r "..\..\eli"
-    File /nonfatal /r "..\..\config"
-    File "..\..\pyproject.toml"
-    File "..\..\eli.bat"
-    File "..\..\install.bat"
-    File "..\..\install.ps1"
-    File /nonfatal "..\..\requirements-windows.txt"
-    CopyFiles /SILENT "$INSTDIR\requirements-windows.txt" "$INSTDIR\requirements.txt"
-
-    SetOutPath "$INSTDIR\dist"
-    File /nonfatal "..\..\dist\eli_mkxi-*.whl"
-
-    SetOutPath "$INSTDIR\wheelhouse"
-    File /nonfatal /r "..\..\dist\wheelhouse\*"
-
-    SetOutPath "$INSTDIR\packaging\assets"
-    File /nonfatal "..\assets\*.ico"
-    File /nonfatal "..\assets\*.png"
-
-    ; Set up venv & deps
-    DetailPrint "Setting up Python venv and installing dependencies (first run only)..."
-    nsExec::ExecToLog '"$INSTDIR\install.bat" /unattended'
-
-    ; Shortcuts
-    CreateDirectory "$SMPROGRAMS\${APPNAME}"
-    CreateShortCut "$SMPROGRAMS\${APPNAME}\${APPNAME}.lnk" "$INSTDIR\${APP_EXE}" "" "$INSTDIR\packaging\assets\eli-mkxi.ico"
-    CreateShortCut "$DESKTOP\${APPNAME}.lnk" "$INSTDIR\${APP_EXE}" "" "$INSTDIR\packaging\assets\eli-mkxi.ico"
-
-    ; Uninstaller
-    WriteUninstaller "$INSTDIR\uninstall.exe"
-    WriteRegStr HKLM "${INSTALL_KEY}" "DisplayName"     "${APPNAME}"
-    WriteRegStr HKLM "${INSTALL_KEY}" "DisplayVersion"  "${VERSION}"
-    WriteRegStr HKLM "${INSTALL_KEY}" "Publisher"       "${COMPANY}"
-    WriteRegStr HKLM "${INSTALL_KEY}" "InstallLocation" "$INSTDIR"
-    WriteRegStr HKLM "${INSTALL_KEY}" "UninstallString" "$INSTDIR\uninstall.exe"
-    WriteRegStr HKLM "${INSTALL_KEY}" "DisplayIcon"     "$INSTDIR\packaging\assets\eli-mkxi.ico"
-SectionEnd
-
-Section "Uninstall"
-    Delete "$SMPROGRAMS\${APPNAME}\${APPNAME}.lnk"
-    RMDir  "$SMPROGRAMS\${APPNAME}"
-    Delete "$DESKTOP\${APPNAME}.lnk"
-
-    Delete "$INSTDIR\uninstall.exe"
-    RMDir /r "$INSTDIR\eli"
-    RMDir /r "$INSTDIR\config"
-    RMDir /r "$INSTDIR\dist"
-    RMDir /r "$INSTDIR\wheelhouse"
-    RMDir /r "$INSTDIR\.venv"
-    RMDir /r "$INSTDIR\packaging"
-    Delete   "$INSTDIR\eli.bat"
-    Delete   "$INSTDIR\install.bat"
-    Delete   "$INSTDIR\install.ps1"
-    Delete   "$INSTDIR\pyproject.toml"
-    Delete   "$INSTDIR\requirements-windows.txt"
-    Delete   "$INSTDIR\requirements.txt"
-    RMDir    "$INSTDIR"
-
-    DeleteRegKey HKLM "${INSTALL_KEY}"
-SectionEnd
diff --git a/tests/test_01_core.py b/tests/test_01_core.py
index 7713ac9..8824a69 100644
--- a/tests/test_01_core.py
+++ b/tests/test_01_core.py
@@ -61,11 +61,6 @@ def test_core_architecture_contracts_loadable():
     assert mod is not None
 
 
-def test_core_first_run_loadable():
-    mod = importlib.import_module("eli.core.first_run")
-    assert mod is not None
-
-
 # ── Cross-module wiring ────────────────────────────────────────────────────────
 
 def test_core_imports_not_circular():
diff --git a/tests/test_planning.py b/tests/test_planning.py
index 72f733c..2d86cf4 100644
--- a/tests/test_planning.py
+++ b/tests/test_planning.py
@@ -174,16 +174,3 @@ def test_habits_memory_recall_returns_dict():
         pass  # may require DB setup
 
 
-# ── First Run ────────────────────────────────────────────────────────────
-
-def test_first_run_importable():
-    from eli.core.first_run import is_first_run
-    assert is_first_run is not None
-
-def test_first_run_returns_bool():
-    from eli.core.first_run import is_first_run
-    try:
-        result = is_first_run()
-        assert isinstance(result, bool)
-    except Exception:
-        pass
