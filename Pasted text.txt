• Verdict
  ELI is a local-first cognitive assistant runtime: a routed tool/action system plus a multi-mode synthesis layer,
  persistent memory, persona overlays, voice/GUI/plugin surfaces, and guarded proactive/autonomy components.
  Your quoted statement is mostly true in implemented architecture, with important caveats on consistency and debt.

  What ELI Is (Concrete)

  - Packaging/entrypoint is GUI-first (eli-mkxi, python -m eli) via pyproject.toml:93 and eli/__main__.py:5.
  - Core orchestrator is eli/kernel/engine.py:6545 with non-Quick grounded middleware blocks around runtime/memory/
    self-report surfaces (eli/kernel/engine.py:6633).
  - Intent routing is explicit and priority-ordered in eli/execution/router_enhanced.py:4514.
  - Action execution is routed through a canonical middleware table in eli/execution/executor_enhanced.py:10852.
  - Grounding/render policy ends in an immutable policy engine install in eli/runtime/
    deterministic_grounding_gate.py:4287.
  - Reasoning modes are real and canonicalized (quick, chain_of_thought, self_consistency, tree_of_thoughts,
    constitutional_ai) in eli/cognition/reasoning_modes.py:6.
  - Multi-agent evidence bus + confidence aggregation exists in eli/cognition/agent_bus.py:1121.
  - Persona continuity is file-backed with dynamic overlay in eli/cognition/persona.py:123 and eli/cognition/
    persona_updater.py:252.
  - World-model state exists and is synced from identity/memory/runtime paths in eli/kernel/world_model.py:61, eli/
    kernel/state.py:221, eli/memory/memory.py:3598, eli/cognition/gguf_inference.py:1067.
  - Proactive/approval-governed automation exists in eli/planning/proactive_daemon.py:40, eli/planning/
    autonomy_controller.py:6, eli/planning/proposal_queue.py:43, eli/planning/goal_store.py:46.
  - Accessibility/voice intent is substantial in eli/perception/audio_stt.py:11 and eli/perception/tts_router.py:273.

  Truth Check Of Your Statement
  interpret intent → True (router + orchestrator stage 1).
  determine when evidence is required → True (need_grounding metadata + engine grounding checks).
  gather/rank grounding → True (parallel retrieval + rerank in orchestrator path).
  synthesize in non-Quick modes → True (mode-specific runners + grounded non-Quick middleware).
  reason under different cognitive modes → True (5 canonical modes).
  preserve persona → True (base persona + auto overlay regeneration).
  expose runtime truths instead of fabricating → Mostly true (strong contracts exist; still depends on specific surface
  quality).
  update memory/self-model/habits where appropriate → True (conversation/memory writes + habit/reflection + overlay
  refresh).
  persistent, local, evolving persona surface → True (not just chat narration).

  Your point that prior description under-described architecture is also correct: ELI is not just memory+chat; it is a
  layered control-plane + cognition runtime.

  Validation I Ran (This Session)

  - pytest -q tests/test_phase71_grounded_surface_contracts.py → 23 passed.
  - pytest -q tests/test_runtime_status_nonquick_no_gguf.py tests/test_recent_memory_processing_no_gguf.py tests/
    test_self_report_recent_updates_no_gguf.py → 7 passed.
  - pytest -q tests/test_eli_contract_routes.py → 10 passed.
  - Plugin audits:
      - tools/audit_plugin_integrity.py → clean.
      - tools/audit_plugin_router_surface.py → plugin actions visible in router.
  - Capability manifest:
      - tools/validate_capability_manifest.py now passes (warnings only), manifest now total=169 at 2026-05-
        16T15:52:57.
      - tools/audit_capability_manifest_against_source.py still reports 5 mismatches (parser/metadata quality debt).

  Current Debt / Inconsistencies

  - Wrapper/patch layering still heavy:
      - execute() redefinitions in executor: 13 occurrences (eli/execution/executor_enhanced.py:7342).
      - render_action() redefinitions in grounding gate: 8 occurrences (eli/runtime/
        deterministic_grounding_gate.py:350).
  - Capability truth surface still noisy:
      - SEQUENCE_STEP and TEMPLATE appear as dispatch-capable in manifest but return unsupported at runtime.
  - Plugin registry drift:
      - Plugin dirs with plugin.py: 11.
      - Registry entries: 7.
      - Unregistered-but-present: calendar, document_reader, media, web_automation.
  - PLUGIN_STATUS health check uses plugins.<id>.plugin import path and reports false errors in this environment (eli/
    execution/executor_enhanced.py:5223, loader is in eli/plugins/manager.py:169).
  - Candidate dead contract module:
      - eli/contracts/grounded_control.py:1 has no call-sites.

  Bottom-Line Answer
  ELI is a local cognitive-agent platform (not just a chatbot) with deterministic routing/execution, grounded evidence
  policies, multi-mode synthesis, persistent memory/persona, voice/GUI control, and guarded proactive autonomy.
  So the claim you posted is substantively true, and yes, earlier phrasing did understate the broader architecture.

  1. If you want, next I can produce the exact cleanup/deletion candidate matrix (file-by-file, risk-graded) without
     deleting anything yet.
  2. Then I can execute your 5-step consolidation plan in order, gated by contract tests after each replacement.

─ Worked for 13m 24s ──────────────────────────────────────────────────────────────────────────────────────────────────


› what about the stages/pipeline? continue with number 1 but no deletions yet


⚠ Heads up, you have less than 25% of your 5h limit left. Run /status for a breakdown.

• Searching the web

• Searched

───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• I’ll build the no-deletion matrix now with a dedicated section for stage/pipeline architecture: router stages,
  executor middleware order, engine/orchestrator flow, and where legacy wrappers still stack. Then I’ll give you a
  risk-graded cleanup plan that only marks candidates.

• Ran python - <<'PY'
  │ import re
  │ from pathlib import Path
  │ … +14 lines
  └ router_priority_stage_count 20
    01. precedence
    … +17 lines (ctrl + t to view transcript)
    19. identity_contract
    20. core_router

• Ran python - <<'PY'
  │ import re
  │ from pathlib import Path
  │ … +11 lines
  └ executor_middleware_count 8
    01. multipdf
    … +5 lines (ctrl + t to view transcript)
    07. profile_scope
    08. identity_only

• Ran python - <<'PY'
  │ from pathlib import Path
  │ import re
  │ … +11 lines
  └ engine_middleware_markers 9
    REASONING_STATUS_V1
    … +6 lines (ctrl + t to view transcript)
    RECENT_MEMORY_PROCESSING_V4
    SELF_REPORT_RECENT_UPDATES_V4

• Ran python - <<'PY'
  │ import re
  │ from pathlib import Path
  │ … +7 lines
  └ orchestrator_trace_stage_entries 17
    stage_1: intent_routing
    … +14 lines (ctrl + t to view transcript)
    stage_9: skipped_nonchat_rerank
    stage_nonchat: executor_observation_loop

• Explored
  └ Search eli/capability_manifest\.json|capability_manifest\.json in eli

• Ran python - <<'PY'
  │ from pathlib import Path
  │ files=['eli/execution/executor_enhanced.py','eli/execution/router_enhanced.py','eli/runtime/
  │ … +11 lines
  └
    --- eli/execution/executor_enhanced.py ---
    … +26 lines (ctrl + t to view transcript)
    type: ignore[no-redef]: 1
    globals().get(: 4

• Ran python - <<'PY'
  │ from pathlib import Path
  │ p=Path('eli/contracts/grounded_control.py')
  │ … +8 lines
  └ exists True lines 210
    eli/contracts/grounded_control.py:162:def evidence_complete_for_action(action: str, local_vars: Mapping[str, Any])
    … +5 lines (ctrl + t to view transcript)
    eli/kernel/engine.py:7826:            if evidence and str(action).upper() in _grounded_control_actions:


• Ran python - <<'PY'
  │ from pathlib import Path
  │ # quick file-level size and function density for hotspot modules
  │ … +10 lines
  └ eli/execution/executor_enhanced.py    lines=10916    defs=274    classes=2
    eli/execution/router_enhanced.py    lines=4803    defs=101    classes=0
    … +2 lines (ctrl + t to view transcript)
    eli/cognition/orchestrator.py    lines=773    defs=17    classes=7
    eli/cognition/agent_bus.py    lines=1834    defs=27    classes=17

• Searching the web

• Searched

• Searching the web

• Searched

───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• Stages/Pipeline (Current As-Built)

  1. Router priority pipeline is explicit and ordered in eli/execution/router_enhanced.py:4514, with 20 stages:
     precedence, memory_runtime_lock, gui_actual_scan, self_report_recent_updates, recent_memory, memory_count,
     profile_scope_explicit, final_memory_contract, runtime_or_name_contract, identity_name_source_contract,
     runtime_cognition_failure_guard, self_improvement_guard, personal_memory_pre_route, lrf_pre_route, portable_route,
     voice_contract, persona_override, followup_passthrough, identity_contract, core_router.
  2. Executor action pipeline is explicit in eli/execution/executor_enhanced.py:10852, with 8 middleware stages:
     multipdf, runtime_status_metadata, self_report_recent_updates, recent_memory_processing, memory_count,
     memory_runtime_sanitizer, profile_scope, identity_only.
  3. Orchestrator CHAT path stages are implemented in eli/cognition/orchestrator.py:463:
     stage_1 intent routing, stage_2 persona lock, stage_3 HyDE, stage_4 planner, stage_5_6_7 retrieval, stage_8 hybrid
     merge, stage_9 rerank, stage_10 context assembly, stage_10_5 persona handoff, stage_11 generation.
  4. Orchestrator non-CHAT flow uses executor observation loop + grounded synthesis path in eli/cognition/
     orchestrator.py:474, skipping retrieval/rerank stages when not needed.
  5. Engine-level grounded middleware markers remain in eli/kernel/engine.py:6633, including non-Quick runtime full-
     pipeline and strict memory/runtime surfaces.

  No-Deletion Cleanup Matrix (Requested #1)
  | Priority | Scope | Evidence | Risk | Recommendation (No deletion yet) |
  |---|---|---|---|---|
  | P0 | Executor wrapper stack | 13 execute() defs and many _PREV/ORIG_EXECUTE chains in eli/execution/
  executor_enhanced.py:7342 | High behavior ambiguity | Collapse legacy redefs into one canonical table path only; keep
  current table as authority. |
  | P0 | Grounding render stack | 8 render_action() redefs and heavy PREVIOUS_RENDER_ACTION chaining in eli/runtime/
  deterministic_grounding_gate.py:350 | High policy drift risk | Keep immutable policy engine as authority, quarantine
  old override layers behind a single legacy adapter. |
  | P0 | Capability truth mismatch tooling | audit_capability_manifest_against_source.py still flags 5 mismatches while
  runtime behavior works for some actions | Medium/High audit false positives | Fix source-audit extraction logic and
  indirect dispatch detection before any deletions. |
  | P1 | Plugin registry drift | 11 plugin dirs vs 7 registry entries (calendar, document_reader, media, web_automation
  not in registry) | Medium operational confusion | Decide policy: either registry is canonical or local dirs are
  canonical; document one source of truth. |
  | P1 | PLUGIN_STATUS false health errors | Import check uses plugins.<id>.plugin in eli/execution/
  executor_enhanced.py:5223 while loader uses package-local import in eli/plugins/manager.py:169 | Medium noisy
  diagnostics | Align health check with actual plugin manager load mechanism. |
  | P1 | Stale duplicate manifest file | Active manifest is project-root capability_manifest.json; stale duplicate
  exists at eli/capability_manifest.json:1 | Medium packaging confusion | Mark stale file as deprecated artifact and
  exclude from runtime/package reads first. |
  | P1 | Unused contract module candidate | eli/contracts/grounded_control.py:1 has no call-sites | Medium dead-code
  risk | Either wire it into engine clarification gate or formally retire after tests are added. |
  | P2 | Custom generated agent residue | eli/brain/agents/custom/tvshowagent.py:1 auto-loads but appears placeholder |
  Low/Medium unintended behavior | Keep but classify as optional/custom; add explicit enable/disable policy before
  removal decisions. |
  | P2 | Phase/legacy annotation density | Very high phase/legacy marker density across core runtime files | Low
  immediate, high maintenance | Create a “phase block index” and map each block to active/inert status before cleanup
  pass. |

  Validation state behind this matrix

  - Grounded contract tests pass: tests/test_phase71_grounded_surface_contracts.py (23/23).
  - Additional targeted grounded tests pass: runtime/memory/self-report non-Quick suites.
  - No files were modified or deleted.

  If you want, next I’ll produce the Phase71+ pipeline freeze checklist as an execution-ready plan (tests to lock
  before consolidation), still with zero deletions.

