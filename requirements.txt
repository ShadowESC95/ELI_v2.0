# ELI MKXI — Runtime requirements (Linux x86_64 with CUDA)
# Generated 2026-04-28 — Python 3.12

# ── Core runtime ──────────────────────────────────────────────────────────────
platformdirs==4.9.6
pydantic==2.13.3
pydantic-settings==2.14.0
pydantic_core==2.46.3
requests==2.33.1
Pillow==12.2.0
mss>=9.0
PyAutoGUI>=0.9.54
python-dotenv==1.2.2
PyYAML==6.0.3
tqdm==4.67.3
psutil==7.2.2
numpy==2.4.4
packaging==26.1
typing_extensions==4.15.0

# ── GUI ───────────────────────────────────────────────────────────────────────
PySide6==6.11.0
PySide6_Addons==6.11.0
PySide6_Essentials==6.11.0
shiboken6==6.11.0

# ── LLM inference ─────────────────────────────────────────────────────────────
llama_cpp_python==0.3.20

# ── Vector store / embeddings ─────────────────────────────────────────────────
faiss-cpu==1.13.2

# ── TTS ───────────────────────────────────────────────────────────────────────
piper-tts==1.4.2
onnxruntime==1.25.0
openwakeword>=0.4.0  # local self-trained wake-word detector (no account)
pyttsx3==2.99
sounddevice==0.5.5
soundfile==0.13.1

# ── ASR / microphone ─────────────────────────────────────────────────────────
faster-whisper==1.2.1
SpeechRecognition==3.16.0
PyAudio==0.2.14

# ── Image generation ─────────────────────────────────────────────────────────
diffusers==0.37.1
safetensors==0.7.0
accelerate==1.13.0
transformers==5.6.2
tokenizers==0.22.2
huggingface_hub==1.11.0

# ── Documents / reports ──────────────────────────────────────────────────────
markdown>=3.5
pdfplumber>=0.10
pypdf>=4.0
python-docx>=1.1

# ── PyTorch (CUDA 12.x) ───────────────────────────────────────────────────────
torch==2.11.0
# Install with: pip install torch --index-url https://download.pytorch.org/whl/cu121

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dateutil==2.9.0.post0
six==1.17.0
filelock==3.29.0
fsspec==2026.3.0
sympy==1.14.0
networkx==3.6.1
Jinja2==3.1.6
MarkupSafe==3.0.3
mpmath==1.3.0
regex==2026.4.4
rich==15.0.0
click==8.3.3
arrow==1.4.0
httpx==0.28.1
certifi==2026.4.22
charset-normalizer==3.4.7
idna==3.13
urllib3==2.6.3

# ── Server + runtime features (one-click parity across platforms; added 2026-06-18) ──
fastapi>=0.110
uvicorn>=0.27
paho-mqtt>=1.6,<3   # ELI-native MQTT device server (no Home Assistant)
zeroconf>=0.70   # mDNS device/broker discovery on the LAN
segno>=1.5   # pure-python QR codes (the web app's "Connect a phone" tab) — no deps
opencv-python>=4.8
pytesseract>=0.3
openwakeword>=0.4
pandas>=2.0
matplotlib>=3.7
huggingface-hub>=0.20
pyflakes>=3.0

# ── Optional feature deps (web search/automation, calendar, notifications) ──
# Match pyproject [full] so requirements installs are equivalent. All degrade
# gracefully if absent; playwright also needs `playwright install` for browsers.
duckduckgo-search>=6.0
playwright>=1.40
plyer>=2.1
ics>=0.7
meshio>=5.3
