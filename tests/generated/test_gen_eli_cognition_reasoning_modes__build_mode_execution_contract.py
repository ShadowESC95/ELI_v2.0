# Auto-generated behavioural test for eli.cognition.reasoning_modes:build_mode_execution_contract
# Generated by eli.runtime.test_generator (Phase 4) — sandbox-verified.
# Review before trusting; regenerate with the testgen task.
import pytest
from eli.cognition.reasoning_modes import build_mode_execution_contract

def test_build_mode_execution_contract():
    # Test with concrete inputs/outputs
    mode = "quick"
    profile = {"base_tokens": 512, "mode_min_tokens": {"quick": 256}}
    runtime_snapshot = {"n_ctx": 2048}
    query_text = "simple question"
    memory_context = "previous context"
    
    expected_output = build_mode_execution_contract(
        mode=mode,
        profile=profile,
        runtime_snapshot=runtime_snapshot,
        query_text=query_text,
        memory_context=memory_context,
    )
    
    actual_output = build_mode_execution_contract(
        mode=mode,
        profile=profile,
        runtime_snapshot=runtime_snapshot,
        query_text=query_text,
        memory_context=memory_context,
    )
    
    assert expected_output == actual_output
