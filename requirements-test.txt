# Test-suite dependencies (cross-platform). Installed by run_tests.sh and
# build_packages.sh pre-flight so subprocess API drivers and lint gates work on
# every developer machine without pulling the full runtime stack.
fastapi>=0.110
uvicorn>=0.27
httpx>=0.27
paho-mqtt>=1.6,<3
pyflakes>=3.0
